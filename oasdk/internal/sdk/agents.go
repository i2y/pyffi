// Code generated by pyffi-gen. DO NOT EDIT.
// Source: Python module "agents"

package agents

import (
	"fmt"

	"github.com/i2y/pyffi"
)

// Module wraps the Python "agents" module.
type Module struct {
	rt  *pyffi.Runtime
	mod *pyffi.Object
}

// New imports the "agents" module and returns a typed wrapper.
func New(rt *pyffi.Runtime) (*Module, error) {
	mod, err := rt.Import("agents")
	if err != nil {
		return nil, err
	}
	return &Module{rt: rt, mod: mod}, nil
}

// Close releases the module reference.
func (m *Module) Close() error {
	return m.mod.Close()
}

// ensure fmt is used
var _ = fmt.Errorf

// AgentHooks wraps agents.AgentHooks.
func (m *Module) AgentHooks() (any, error) {
	fn := m.mod.Attr("AgentHooks")
	if fn == nil {
		return nil, fmt.Errorf("AgentHooks not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Button wraps agents.Button.
func (m *Module) Button() (any, error) {
	fn := m.mod.Attr("Button")
	if fn == nil {
		return nil, fmt.Errorf("Button not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// DynamicPromptFunction wraps agents.DynamicPromptFunction.
func (m *Module) DynamicPromptFunction() (any, error) {
	fn := m.mod.Attr("DynamicPromptFunction")
	if fn == nil {
		return nil, fmt.Errorf("DynamicPromptFunction not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Environment wraps agents.Environment.
func (m *Module) Environment() (any, error) {
	fn := m.mod.Attr("Environment")
	if fn == nil {
		return nil, fmt.Errorf("Environment not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// HandoffInputFilter wraps agents.HandoffInputFilter.
func (m *Module) HandoffInputFilter() (any, error) {
	fn := m.mod.Attr("HandoffInputFilter")
	if fn == nil {
		return nil, fmt.Errorf("HandoffInputFilter not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Literal wraps agents.Literal.
// Special typing form to define literal types (a.k.a. value types).
func (m *Module) Literal() (any, error) {
	fn := m.mod.Attr("Literal")
	if fn == nil {
		return nil, fmt.Errorf("Literal not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// LocalShellExecutor wraps agents.LocalShellExecutor.
func (m *Module) LocalShellExecutor() (any, error) {
	fn := m.mod.Attr("LocalShellExecutor")
	if fn == nil {
		return nil, fmt.Errorf("LocalShellExecutor not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// MCPToolApprovalFunction wraps agents.MCPToolApprovalFunction.
func (m *Module) MCPToolApprovalFunction() (any, error) {
	fn := m.mod.Attr("MCPToolApprovalFunction")
	if fn == nil {
		return nil, fmt.Errorf("MCPToolApprovalFunction not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ReasoningItemIdPolicy wraps agents.ReasoningItemIdPolicy.
func (m *Module) ReasoningItemIdPolicy() (any, error) {
	fn := m.mod.Attr("ReasoningItemIdPolicy")
	if fn == nil {
		return nil, fmt.Errorf("ReasoningItemIdPolicy not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RetryPolicy wraps agents.RetryPolicy.
func (m *Module) RetryPolicy() (any, error) {
	fn := m.mod.Attr("RetryPolicy")
	if fn == nil {
		return nil, fmt.Errorf("RetryPolicy not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunErrorHandler wraps agents.RunErrorHandler.
func (m *Module) RunErrorHandler() (any, error) {
	fn := m.mod.Attr("RunErrorHandler")
	if fn == nil {
		return nil, fmt.Errorf("RunErrorHandler not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunHooks wraps agents.RunHooks.
func (m *Module) RunHooks() (any, error) {
	fn := m.mod.Attr("RunHooks")
	if fn == nil {
		return nil, fmt.Errorf("RunHooks not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ShellExecutor wraps agents.ShellExecutor.
func (m *Module) ShellExecutor() (any, error) {
	fn := m.mod.Attr("ShellExecutor")
	if fn == nil {
		return nil, fmt.Errorf("ShellExecutor not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolErrorFormatter wraps agents.ToolErrorFormatter.
func (m *Module) ToolErrorFormatter() (any, error) {
	fn := m.mod.Attr("ToolErrorFormatter")
	if fn == nil {
		return nil, fmt.Errorf("ToolErrorFormatter not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolsToFinalOutputFunction wraps agents.ToolsToFinalOutputFunction.
func (m *Module) ToolsToFinalOutputFunction() (any, error) {
	fn := m.mod.Attr("ToolsToFinalOutputFunction")
	if fn == nil {
		return nil, fmt.Errorf("ToolsToFinalOutputFunction not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// AddTraceProcessor wraps agents.add_trace_processor.
// Adds a new trace processor. This processor will receive all traces/spans.
func (m *Module) AddTraceProcessor(spanProcessor *TracingProcessor) error {
	fn := m.mod.Attr("add_trace_processor")
	if fn == nil {
		return fmt.Errorf("add_trace_processor not found")
	}
	defer fn.Close()
	result, err := fn.Call(spanProcessor)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// AgentSpan wraps agents.agent_span.
// Create a new agent span. The span will not be started automatically, you should either do
func (m *Module) AgentSpan(name string, opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("agent_span")
	if fn == nil {
		return nil, fmt.Errorf("agent_span not found")
	}
	defer fn.Close()
	args := []any{name}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ApplyDiff wraps agents.apply_diff.
// Apply a V4A diff to the provided text.
func (m *Module) ApplyDiff(input string, diff string, opts ...pyffi.KW) (string, error) {
	fn := m.mod.Attr("apply_diff")
	if fn == nil {
		return "", fmt.Errorf("apply_diff not found")
	}
	defer fn.Close()
	args := []any{input, diff}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// CustomSpan wraps agents.custom_span.
// Create a new custom span, to which you can add your own metadata. The span will not be
func (m *Module) CustomSpan(name string, opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("custom_span")
	if fn == nil {
		return nil, fmt.Errorf("custom_span not found")
	}
	defer fn.Close()
	args := []any{name}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// DefaultHandoffHistoryMapper wraps agents.default_handoff_history_mapper.
// Return a single assistant message summarizing the transcript.
func (m *Module) DefaultHandoffHistoryMapper(transcript any) (any, error) {
	fn := m.mod.Attr("default_handoff_history_mapper")
	if fn == nil {
		return nil, fmt.Errorf("default_handoff_history_mapper not found")
	}
	defer fn.Close()
	result, err := fn.Call(transcript)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// DefaultToolErrorFunction wraps agents.default_tool_error_function.
// The default tool error function, which just returns a generic error message.
func (m *Module) DefaultToolErrorFunction(ctx any, error any) (string, error) {
	fn := m.mod.Attr("default_tool_error_function")
	if fn == nil {
		return "", fmt.Errorf("default_tool_error_function not found")
	}
	defer fn.Close()
	result, err := fn.Call(ctx, error)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// DisposeResolvedComputers wraps agents.dispose_resolved_computers.
// Dispose any computer instances created for the provided run context.
func (m *Module) DisposeResolvedComputers(opts ...pyffi.KW) error {
	fn := m.mod.Attr("dispose_resolved_computers")
	if fn == nil {
		return fmt.Errorf("dispose_resolved_computers not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// EnableVerboseStdoutLogging wraps agents.enable_verbose_stdout_logging.
// Enables verbose logging to stdout. This is useful for debugging.
func (m *Module) EnableVerboseStdoutLogging() (any, error) {
	fn := m.mod.Attr("enable_verbose_stdout_logging")
	if fn == nil {
		return nil, fmt.Errorf("enable_verbose_stdout_logging not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// FunctionSpan wraps agents.function_span.
// Create a new function span. The span will not be started automatically, you should either do
func (m *Module) FunctionSpan(name string, opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("function_span")
	if fn == nil {
		return nil, fmt.Errorf("function_span not found")
	}
	defer fn.Close()
	args := []any{name}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// FunctionTool wraps agents.function_tool.
// Decorator to create a FunctionTool from a function. By default, we will:
func (m *Module) FunctionTool(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("function_tool")
	if fn == nil {
		return nil, fmt.Errorf("function_tool not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GenSpanId wraps agents.gen_span_id.
// Generate a new span ID.
func (m *Module) GenSpanId() (string, error) {
	fn := m.mod.Attr("gen_span_id")
	if fn == nil {
		return "", fmt.Errorf("gen_span_id not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// GenTraceId wraps agents.gen_trace_id.
// Generate a new trace ID.
func (m *Module) GenTraceId() (string, error) {
	fn := m.mod.Attr("gen_trace_id")
	if fn == nil {
		return "", fmt.Errorf("gen_trace_id not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// GenerationSpan wraps agents.generation_span.
// Create a new generation span. The span will not be started automatically, you should either
func (m *Module) GenerationSpan(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("generation_span")
	if fn == nil {
		return nil, fmt.Errorf("generation_span not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetConversationHistoryWrappers wraps agents.get_conversation_history_wrappers.
// Return the current start/end markers used for the nested conversation summary.
func (m *Module) GetConversationHistoryWrappers() (any, error) {
	fn := m.mod.Attr("get_conversation_history_wrappers")
	if fn == nil {
		return nil, fmt.Errorf("get_conversation_history_wrappers not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetCurrentSpan wraps agents.get_current_span.
// Returns the currently active span, if present.
func (m *Module) GetCurrentSpan() (any, error) {
	fn := m.mod.Attr("get_current_span")
	if fn == nil {
		return nil, fmt.Errorf("get_current_span not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetCurrentTrace wraps agents.get_current_trace.
// Returns the currently active trace, if present.
func (m *Module) GetCurrentTrace() (*Trace, error) {
	fn := m.mod.Attr("get_current_trace")
	if fn == nil {
		return nil, fmt.Errorf("get_current_trace not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	return &Trace{obj: result, rt: m.rt}, nil
}

// GuardrailSpan wraps agents.guardrail_span.
// Create a new guardrail span. The span will not be started automatically, you should either
func (m *Module) GuardrailSpan(name string, opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("guardrail_span")
	if fn == nil {
		return nil, fmt.Errorf("guardrail_span not found")
	}
	defer fn.Close()
	args := []any{name}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Handoff wraps agents.handoff.
// Create a handoff from an agent.
func (m *Module) Handoff(agent any, opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("handoff")
	if fn == nil {
		return nil, fmt.Errorf("handoff not found")
	}
	defer fn.Close()
	args := []any{agent}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// HandoffSpan wraps agents.handoff_span.
// Create a new handoff span. The span will not be started automatically, you should either do
func (m *Module) HandoffSpan(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("handoff_span")
	if fn == nil {
		return nil, fmt.Errorf("handoff_span not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// InputGuardrail wraps agents.input_guardrail.
// Decorator that transforms a sync or async function into an `InputGuardrail`.
func (m *Module) InputGuardrail(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("input_guardrail")
	if fn == nil {
		return nil, fmt.Errorf("input_guardrail not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// IsOpenaiResponsesCompactionAwareSession wraps agents.is_openai_responses_compaction_aware_session.
// Check if a session supports responses compaction.
func (m *Module) IsOpenaiResponsesCompactionAwareSession(session *Session) (any, error) {
	fn := m.mod.Attr("is_openai_responses_compaction_aware_session")
	if fn == nil {
		return nil, fmt.Errorf("is_openai_responses_compaction_aware_session not found")
	}
	defer fn.Close()
	result, err := fn.Call(session)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// McpToolsSpan wraps agents.mcp_tools_span.
// Create a new MCP list tools span. The span will not be started automatically, you should
func (m *Module) McpToolsSpan(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("mcp_tools_span")
	if fn == nil {
		return nil, fmt.Errorf("mcp_tools_span not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// NestHandoffHistory wraps agents.nest_handoff_history.
// Summarize the previous transcript for the next agent.
func (m *Module) NestHandoffHistory(handoffInputData *HandoffInputData, opts ...pyffi.KW) (*HandoffInputData, error) {
	fn := m.mod.Attr("nest_handoff_history")
	if fn == nil {
		return nil, fmt.Errorf("nest_handoff_history not found")
	}
	defer fn.Close()
	args := []any{handoffInputData}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	return &HandoffInputData{obj: result, rt: m.rt}, nil
}

// OutputGuardrail wraps agents.output_guardrail.
// Decorator that transforms a sync or async function into an `OutputGuardrail`.
func (m *Module) OutputGuardrail(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("output_guardrail")
	if fn == nil {
		return nil, fmt.Errorf("output_guardrail not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ResetConversationHistoryWrappers wraps agents.reset_conversation_history_wrappers.
// Restore the default “<CONVERSATION HISTORY>“ markers.
func (m *Module) ResetConversationHistoryWrappers() error {
	fn := m.mod.Attr("reset_conversation_history_wrappers")
	if fn == nil {
		return fmt.Errorf("reset_conversation_history_wrappers not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ResolveComputer wraps agents.resolve_computer.
// Resolve a computer for a given run context, initializing it if needed.
func (m *Module) ResolveComputer(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("resolve_computer")
	if fn == nil {
		return nil, fmt.Errorf("resolve_computer not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ResponsesWebsocketSession wraps agents.responses_websocket_session.
// Create a shared OpenAI Responses websocket session for multiple Runner calls.
func (m *Module) ResponsesWebsocketSession(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("responses_websocket_session")
	if fn == nil {
		return nil, fmt.Errorf("responses_websocket_session not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunDemoLoop wraps agents.run_demo_loop.
// Run a simple REPL loop with the given agent.
func (m *Module) RunDemoLoop(agent any, opts ...pyffi.KW) error {
	fn := m.mod.Attr("run_demo_loop")
	if fn == nil {
		return fmt.Errorf("run_demo_loop not found")
	}
	defer fn.Close()
	args := []any{agent}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetConversationHistoryWrappers wraps agents.set_conversation_history_wrappers.
// Override the markers that wrap the generated conversation summary.
func (m *Module) SetConversationHistoryWrappers(opts ...pyffi.KW) error {
	fn := m.mod.Attr("set_conversation_history_wrappers")
	if fn == nil {
		return fmt.Errorf("set_conversation_history_wrappers not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetDefaultOpenaiApi wraps agents.set_default_openai_api.
// Set the default API to use for OpenAI LLM requests. By default, we will use the responses API
func (m *Module) SetDefaultOpenaiApi(api any) error {
	fn := m.mod.Attr("set_default_openai_api")
	if fn == nil {
		return fmt.Errorf("set_default_openai_api not found")
	}
	defer fn.Close()
	result, err := fn.Call(api)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetDefaultOpenaiClient wraps agents.set_default_openai_client.
// Set the default OpenAI client to use for LLM requests and/or tracing. If provided, this
func (m *Module) SetDefaultOpenaiClient(client *AsyncOpenAI, opts ...pyffi.KW) error {
	fn := m.mod.Attr("set_default_openai_client")
	if fn == nil {
		return fmt.Errorf("set_default_openai_client not found")
	}
	defer fn.Close()
	args := []any{client}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetDefaultOpenaiKey wraps agents.set_default_openai_key.
// Set the default OpenAI API key to use for LLM requests (and optionally tracing()). This is
func (m *Module) SetDefaultOpenaiKey(key string, opts ...pyffi.KW) error {
	fn := m.mod.Attr("set_default_openai_key")
	if fn == nil {
		return fmt.Errorf("set_default_openai_key not found")
	}
	defer fn.Close()
	args := []any{key}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetDefaultOpenaiResponsesTransport wraps agents.set_default_openai_responses_transport.
// Set the default transport for OpenAI Responses API requests.
func (m *Module) SetDefaultOpenaiResponsesTransport(transport any) error {
	fn := m.mod.Attr("set_default_openai_responses_transport")
	if fn == nil {
		return fmt.Errorf("set_default_openai_responses_transport not found")
	}
	defer fn.Close()
	result, err := fn.Call(transport)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetTraceProcessors wraps agents.set_trace_processors.
// Set the list of trace processors. This will replace the current list of processors.
func (m *Module) SetTraceProcessors(processors any) error {
	fn := m.mod.Attr("set_trace_processors")
	if fn == nil {
		return fmt.Errorf("set_trace_processors not found")
	}
	defer fn.Close()
	result, err := fn.Call(processors)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetTraceProvider wraps agents.set_trace_provider.
// Set the global trace provider used by tracing utilities.
func (m *Module) SetTraceProvider(provider any) error {
	fn := m.mod.Attr("set_trace_provider")
	if fn == nil {
		return fmt.Errorf("set_trace_provider not found")
	}
	defer fn.Close()
	result, err := fn.Call(provider)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetTracingDisabled wraps agents.set_tracing_disabled.
// Set whether tracing is globally disabled.
func (m *Module) SetTracingDisabled(disabled bool) error {
	fn := m.mod.Attr("set_tracing_disabled")
	if fn == nil {
		return fmt.Errorf("set_tracing_disabled not found")
	}
	defer fn.Close()
	result, err := fn.Call(disabled)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetTracingExportApiKey wraps agents.set_tracing_export_api_key.
// Set the OpenAI API key for the backend exporter.
func (m *Module) SetTracingExportApiKey(apiKey string) error {
	fn := m.mod.Attr("set_tracing_export_api_key")
	if fn == nil {
		return fmt.Errorf("set_tracing_export_api_key not found")
	}
	defer fn.Close()
	result, err := fn.Call(apiKey)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SpeechGroupSpan wraps agents.speech_group_span.
// Create a new speech group span. The span will not be started automatically, you should
func (m *Module) SpeechGroupSpan(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("speech_group_span")
	if fn == nil {
		return nil, fmt.Errorf("speech_group_span not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// SpeechSpan wraps agents.speech_span.
// Create a new speech span. The span will not be started automatically, you should either do
func (m *Module) SpeechSpan(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("speech_span")
	if fn == nil {
		return nil, fmt.Errorf("speech_span not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolInputGuardrail wraps agents.tool_input_guardrail.
// Decorator to create a ToolInputGuardrail from a function.
func (m *Module) ToolInputGuardrail(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("tool_input_guardrail")
	if fn == nil {
		return nil, fmt.Errorf("tool_input_guardrail not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolNamespace wraps agents.tool_namespace.
// Attach namespace metadata to function tools for OpenAI Responses tool search.
func (m *Module) ToolNamespace(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("tool_namespace")
	if fn == nil {
		return nil, fmt.Errorf("tool_namespace not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolOutputGuardrail wraps agents.tool_output_guardrail.
// Decorator to create a ToolOutputGuardrail from a function.
func (m *Module) ToolOutputGuardrail(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("tool_output_guardrail")
	if fn == nil {
		return nil, fmt.Errorf("tool_output_guardrail not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Trace wraps agents.trace.
// Create a new trace. The trace will not be started automatically; you should either use
func (m *Module) Trace(workflowName string, opts ...pyffi.KW) (*Trace, error) {
	fn := m.mod.Attr("trace")
	if fn == nil {
		return nil, fmt.Errorf("trace not found")
	}
	defer fn.Close()
	args := []any{workflowName}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	return &Trace{obj: result, rt: m.rt}, nil
}

// TranscriptionSpan wraps agents.transcription_span.
// Create a new transcription span. The span will not be started automatically, you should
func (m *Module) TranscriptionSpan(opts ...pyffi.KW) (any, error) {
	fn := m.mod.Attr("transcription_span")
	if fn == nil {
		return nil, fmt.Errorf("transcription_span not found")
	}
	defer fn.Close()
	args := []any{}
	var kw pyffi.KW
	if len(opts) > 0 {
		kw = opts[0]
	}
	result, err := fn.CallKw(args, kw)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Agent wraps a Python Agent instance.
// An agent is an AI model configured with instructions, tools, guardrails, handoffs and more.
type Agent struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgent creates a new Agent instance.
func (m *Module) NewAgent(name string, handoffDescription any, tools any, mcpServers any, mcpConfig any, instructions any, prompt any, handoffs any, model any, modelSettings *ModelSettings, inputGuardrails any, outputGuardrails any, outputType any, hooks any, toolUseBehavior any, resetToolChoice bool) (*Agent, error) {
	cls := m.mod.Attr("Agent")
	if cls == nil {
		return nil, fmt.Errorf("class Agent not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, handoffDescription, tools, mcpServers, mcpConfig, instructions, prompt, handoffs, model, modelSettings, inputGuardrails, outputGuardrails, outputType, hooks, toolUseBehavior, resetToolChoice)
	if err != nil {
		return nil, err
	}
	return &Agent{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Agent) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Agent) Object() *pyffi.Object {
	return w.obj
}

// AsTool wraps Agent.as_tool.
// Transform this agent into a tool, callable by other agents.
func (w *Agent) AsTool(toolName any, toolDescription any, customOutputExtractor any, isEnabled any, onStream any, runConfig *RunConfig, maxTurns any, hooks any, previousResponseId any, conversationId any, session *Session, failureErrorFunction any, needsApproval any, parameters any, inputBuilder any, includeInputSchema bool) (*FunctionTool, error) {
	fn := w.obj.Attr("as_tool")
	if fn == nil {
		return nil, fmt.Errorf("as_tool not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, toolDescription, customOutputExtractor, isEnabled, onStream, runConfig, maxTurns, hooks, previousResponseId, conversationId, session, failureErrorFunction, needsApproval, parameters, inputBuilder, includeInputSchema)
	if err != nil {
		return nil, err
	}
	return &FunctionTool{obj: result, rt: w.rt}, nil
}

// Clone wraps Agent.clone.
// Make a copy of the agent, with the given arguments changed.
func (w *Agent) Clone() (any, error) {
	fn := w.obj.Attr("clone")
	if fn == nil {
		return nil, fmt.Errorf("clone not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetAllTools wraps Agent.get_all_tools.
// All agent tools, including MCP tools and function tools.
func (w *Agent) GetAllTools(runContext any) (any, error) {
	fn := w.obj.Attr("get_all_tools")
	if fn == nil {
		return nil, fmt.Errorf("get_all_tools not found")
	}
	defer fn.Close()
	result, err := fn.Call(runContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetMcpTools wraps Agent.get_mcp_tools.
// Fetches the available tools from the MCP servers.
func (w *Agent) GetMcpTools(runContext any) (any, error) {
	fn := w.obj.Attr("get_mcp_tools")
	if fn == nil {
		return nil, fmt.Errorf("get_mcp_tools not found")
	}
	defer fn.Close()
	result, err := fn.Call(runContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetPrompt wraps Agent.get_prompt.
// Get the prompt for the agent.
func (w *Agent) GetPrompt(runContext any) (any, error) {
	fn := w.obj.Attr("get_prompt")
	if fn == nil {
		return nil, fmt.Errorf("get_prompt not found")
	}
	defer fn.Close()
	result, err := fn.Call(runContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetSystemPrompt wraps Agent.get_system_prompt.
func (w *Agent) GetSystemPrompt(runContext any) (any, error) {
	fn := w.obj.Attr("get_system_prompt")
	if fn == nil {
		return nil, fmt.Errorf("get_system_prompt not found")
	}
	defer fn.Close()
	result, err := fn.Call(runContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// AgentBase wraps a Python AgentBase instance.
// Base class for `Agent` and `RealtimeAgent`.
type AgentBase struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentBase creates a new AgentBase instance.
func (m *Module) NewAgentBase(name string, handoffDescription any, tools any, mcpServers any, mcpConfig any) (*AgentBase, error) {
	cls := m.mod.Attr("AgentBase")
	if cls == nil {
		return nil, fmt.Errorf("class AgentBase not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, handoffDescription, tools, mcpServers, mcpConfig)
	if err != nil {
		return nil, err
	}
	return &AgentBase{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentBase) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentBase) Object() *pyffi.Object {
	return w.obj
}

// GetAllTools wraps AgentBase.get_all_tools.
// All agent tools, including MCP tools and function tools.
func (w *AgentBase) GetAllTools(runContext any) (any, error) {
	fn := w.obj.Attr("get_all_tools")
	if fn == nil {
		return nil, fmt.Errorf("get_all_tools not found")
	}
	defer fn.Close()
	result, err := fn.Call(runContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetMcpTools wraps AgentBase.get_mcp_tools.
// Fetches the available tools from the MCP servers.
func (w *AgentBase) GetMcpTools(runContext any) (any, error) {
	fn := w.obj.Attr("get_mcp_tools")
	if fn == nil {
		return nil, fmt.Errorf("get_mcp_tools not found")
	}
	defer fn.Close()
	result, err := fn.Call(runContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// AgentHookContext wraps a Python AgentHookContext instance.
// Context passed to agent hooks (on_start, on_end).
type AgentHookContext struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentHookContext creates a new AgentHookContext instance.
func (m *Module) NewAgentHookContext(context any, usage *Usage, turnInput any, Approvals any, toolInput any) (*AgentHookContext, error) {
	cls := m.mod.Attr("AgentHookContext")
	if cls == nil {
		return nil, fmt.Errorf("class AgentHookContext not found")
	}
	defer cls.Close()
	obj, err := cls.Call(context, usage, turnInput, Approvals, toolInput)
	if err != nil {
		return nil, err
	}
	return &AgentHookContext{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentHookContext) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentHookContext) Object() *pyffi.Object {
	return w.obj
}

// ApproveTool wraps AgentHookContext.approve_tool.
// Approve a tool call, optionally for all future calls.
func (w *AgentHookContext) ApproveTool(approvalItem any, alwaysApprove bool) error {
	fn := w.obj.Attr("approve_tool")
	if fn == nil {
		return fmt.Errorf("approve_tool not found")
	}
	defer fn.Close()
	result, err := fn.Call(approvalItem, alwaysApprove)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetApprovalStatus wraps AgentHookContext.get_approval_status.
// Return approval status, retrying with pending item's tool name if necessary.
func (w *AgentHookContext) GetApprovalStatus(toolName string, callId string, toolNamespace any, existingPending any, toolLookupKey any) (any, error) {
	fn := w.obj.Attr("get_approval_status")
	if fn == nil {
		return nil, fmt.Errorf("get_approval_status not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, callId, toolNamespace, existingPending, toolLookupKey)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetRejectionMessage wraps AgentHookContext.get_rejection_message.
// Return a stored rejection message for a tool call if one exists.
func (w *AgentHookContext) GetRejectionMessage(toolName string, callId string, toolNamespace any, existingPending any, toolLookupKey any) (any, error) {
	fn := w.obj.Attr("get_rejection_message")
	if fn == nil {
		return nil, fmt.Errorf("get_rejection_message not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, callId, toolNamespace, existingPending, toolLookupKey)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// IsToolApproved wraps AgentHookContext.is_tool_approved.
// Return True/False/None for the given tool call.
func (w *AgentHookContext) IsToolApproved(toolName string, callId string) (any, error) {
	fn := w.obj.Attr("is_tool_approved")
	if fn == nil {
		return nil, fmt.Errorf("is_tool_approved not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, callId)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RejectTool wraps AgentHookContext.reject_tool.
// Reject a tool call, optionally for all future calls.
func (w *AgentHookContext) RejectTool(approvalItem any, alwaysReject bool, rejectionMessage any) error {
	fn := w.obj.Attr("reject_tool")
	if fn == nil {
		return fmt.Errorf("reject_tool not found")
	}
	defer fn.Close()
	result, err := fn.Call(approvalItem, alwaysReject, rejectionMessage)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// AgentOutputSchema wraps a Python AgentOutputSchema instance.
// An object that captures the JSON schema of the output, as well as validating/parsing JSON
type AgentOutputSchema struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentOutputSchema creates a new AgentOutputSchema instance.
func (m *Module) NewAgentOutputSchema(outputType any, strictJsonSchema bool) (*AgentOutputSchema, error) {
	cls := m.mod.Attr("AgentOutputSchema")
	if cls == nil {
		return nil, fmt.Errorf("class AgentOutputSchema not found")
	}
	defer cls.Close()
	obj, err := cls.Call(outputType, strictJsonSchema)
	if err != nil {
		return nil, err
	}
	return &AgentOutputSchema{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentOutputSchema) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentOutputSchema) Object() *pyffi.Object {
	return w.obj
}

// IsPlainText wraps AgentOutputSchema.is_plain_text.
// Whether the output type is plain text (versus a JSON object).
func (w *AgentOutputSchema) IsPlainText() (bool, error) {
	fn := w.obj.Attr("is_plain_text")
	if fn == nil {
		return false, fmt.Errorf("is_plain_text not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return false, err
	}
	defer result.Close()
	return result.Bool()
}

// IsStrictJsonSchema wraps AgentOutputSchema.is_strict_json_schema.
// Whether the JSON schema is in strict mode.
func (w *AgentOutputSchema) IsStrictJsonSchema() (bool, error) {
	fn := w.obj.Attr("is_strict_json_schema")
	if fn == nil {
		return false, fmt.Errorf("is_strict_json_schema not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return false, err
	}
	defer result.Close()
	return result.Bool()
}

// JsonSchema wraps AgentOutputSchema.json_schema.
// The JSON schema of the output type.
func (w *AgentOutputSchema) JsonSchema() (any, error) {
	fn := w.obj.Attr("json_schema")
	if fn == nil {
		return nil, fmt.Errorf("json_schema not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Name wraps AgentOutputSchema.name.
// The name of the output type.
func (w *AgentOutputSchema) Name() (string, error) {
	fn := w.obj.Attr("name")
	if fn == nil {
		return "", fmt.Errorf("name not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ValidateJson wraps AgentOutputSchema.validate_json.
// Validate a JSON string against the output type. Returns the validated object, or raises
func (w *AgentOutputSchema) ValidateJson(jsonStr string) (any, error) {
	fn := w.obj.Attr("validate_json")
	if fn == nil {
		return nil, fmt.Errorf("validate_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(jsonStr)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// AgentOutputSchemaBase wraps a Python AgentOutputSchemaBase instance.
// An object that captures the JSON schema of the output, as well as validating/parsing JSON
type AgentOutputSchemaBase struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentOutputSchemaBase creates a new AgentOutputSchemaBase instance.
func (m *Module) NewAgentOutputSchemaBase() (*AgentOutputSchemaBase, error) {
	cls := m.mod.Attr("AgentOutputSchemaBase")
	if cls == nil {
		return nil, fmt.Errorf("class AgentOutputSchemaBase not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &AgentOutputSchemaBase{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentOutputSchemaBase) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentOutputSchemaBase) Object() *pyffi.Object {
	return w.obj
}

// IsPlainText wraps AgentOutputSchemaBase.is_plain_text.
// Whether the output type is plain text (versus a JSON object).
func (w *AgentOutputSchemaBase) IsPlainText() (bool, error) {
	fn := w.obj.Attr("is_plain_text")
	if fn == nil {
		return false, fmt.Errorf("is_plain_text not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return false, err
	}
	defer result.Close()
	return result.Bool()
}

// IsStrictJsonSchema wraps AgentOutputSchemaBase.is_strict_json_schema.
// Whether the JSON schema is in strict mode. Strict mode constrains the JSON schema
func (w *AgentOutputSchemaBase) IsStrictJsonSchema() (bool, error) {
	fn := w.obj.Attr("is_strict_json_schema")
	if fn == nil {
		return false, fmt.Errorf("is_strict_json_schema not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return false, err
	}
	defer result.Close()
	return result.Bool()
}

// JsonSchema wraps AgentOutputSchemaBase.json_schema.
// Returns the JSON schema of the output. Will only be called if the output type is not
func (w *AgentOutputSchemaBase) JsonSchema() (any, error) {
	fn := w.obj.Attr("json_schema")
	if fn == nil {
		return nil, fmt.Errorf("json_schema not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Name wraps AgentOutputSchemaBase.name.
// The name of the output type.
func (w *AgentOutputSchemaBase) Name() (string, error) {
	fn := w.obj.Attr("name")
	if fn == nil {
		return "", fmt.Errorf("name not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ValidateJson wraps AgentOutputSchemaBase.validate_json.
// Validate a JSON string against the output type. You must return the validated object,
func (w *AgentOutputSchemaBase) ValidateJson(jsonStr string) (any, error) {
	fn := w.obj.Attr("validate_json")
	if fn == nil {
		return nil, fmt.Errorf("validate_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(jsonStr)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// AgentSpanData wraps a Python AgentSpanData instance.
// Represents an Agent Span in the trace.
type AgentSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentSpanData creates a new AgentSpanData instance.
func (m *Module) NewAgentSpanData(name string, handoffs any, tools any, outputType any) (*AgentSpanData, error) {
	cls := m.mod.Attr("AgentSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class AgentSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, handoffs, tools, outputType)
	if err != nil {
		return nil, err
	}
	return &AgentSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps AgentSpanData.export.
// Export the span data as a dictionary.
func (w *AgentSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// AgentToolInvocation wraps a Python AgentToolInvocation instance.
// Immutable metadata about a nested agent-tool invocation.
type AgentToolInvocation struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentToolInvocation creates a new AgentToolInvocation instance.
func (m *Module) NewAgentToolInvocation(toolName string, toolCallId string, toolArguments string) (*AgentToolInvocation, error) {
	cls := m.mod.Attr("AgentToolInvocation")
	if cls == nil {
		return nil, fmt.Errorf("class AgentToolInvocation not found")
	}
	defer cls.Close()
	obj, err := cls.Call(toolName, toolCallId, toolArguments)
	if err != nil {
		return nil, err
	}
	return &AgentToolInvocation{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentToolInvocation) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentToolInvocation) Object() *pyffi.Object {
	return w.obj
}

// AgentToolStreamEvent wraps a Python AgentToolStreamEvent instance.
// Streaming event emitted when an agent is invoked as a tool.
type AgentToolStreamEvent struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentToolStreamEvent creates a new AgentToolStreamEvent instance.
func (m *Module) NewAgentToolStreamEvent() (*AgentToolStreamEvent, error) {
	cls := m.mod.Attr("AgentToolStreamEvent")
	if cls == nil {
		return nil, fmt.Errorf("class AgentToolStreamEvent not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &AgentToolStreamEvent{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentToolStreamEvent) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentToolStreamEvent) Object() *pyffi.Object {
	return w.obj
}

// AgentUpdatedStreamEvent wraps a Python AgentUpdatedStreamEvent instance.
// Event that notifies that there is a new agent running.
type AgentUpdatedStreamEvent struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentUpdatedStreamEvent creates a new AgentUpdatedStreamEvent instance.
func (m *Module) NewAgentUpdatedStreamEvent(newAgent any, type_ any) (*AgentUpdatedStreamEvent, error) {
	cls := m.mod.Attr("AgentUpdatedStreamEvent")
	if cls == nil {
		return nil, fmt.Errorf("class AgentUpdatedStreamEvent not found")
	}
	defer cls.Close()
	obj, err := cls.Call(newAgent, type_)
	if err != nil {
		return nil, err
	}
	return &AgentUpdatedStreamEvent{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentUpdatedStreamEvent) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentUpdatedStreamEvent) Object() *pyffi.Object {
	return w.obj
}

// AgentsException wraps a Python AgentsException instance.
// Base class for all exceptions in the Agents SDK.
type AgentsException struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAgentsException creates a new AgentsException instance.
func (m *Module) NewAgentsException() (*AgentsException, error) {
	cls := m.mod.Attr("AgentsException")
	if cls == nil {
		return nil, fmt.Errorf("class AgentsException not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &AgentsException{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AgentsException) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AgentsException) Object() *pyffi.Object {
	return w.obj
}

// ApplyPatchEditor wraps a Python ApplyPatchEditor instance.
// Host-defined editor that applies diffs on disk.
type ApplyPatchEditor struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewApplyPatchEditor creates a new ApplyPatchEditor instance.
func (m *Module) NewApplyPatchEditor() (*ApplyPatchEditor, error) {
	cls := m.mod.Attr("ApplyPatchEditor")
	if cls == nil {
		return nil, fmt.Errorf("class ApplyPatchEditor not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ApplyPatchEditor{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ApplyPatchEditor) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ApplyPatchEditor) Object() *pyffi.Object {
	return w.obj
}

// CreateFile wraps ApplyPatchEditor.create_file.
func (w *ApplyPatchEditor) CreateFile(operation *ApplyPatchOperation) (any, error) {
	fn := w.obj.Attr("create_file")
	if fn == nil {
		return nil, fmt.Errorf("create_file not found")
	}
	defer fn.Close()
	result, err := fn.Call(operation)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// DeleteFile wraps ApplyPatchEditor.delete_file.
func (w *ApplyPatchEditor) DeleteFile(operation *ApplyPatchOperation) (any, error) {
	fn := w.obj.Attr("delete_file")
	if fn == nil {
		return nil, fmt.Errorf("delete_file not found")
	}
	defer fn.Close()
	result, err := fn.Call(operation)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// UpdateFile wraps ApplyPatchEditor.update_file.
func (w *ApplyPatchEditor) UpdateFile(operation *ApplyPatchOperation) (any, error) {
	fn := w.obj.Attr("update_file")
	if fn == nil {
		return nil, fmt.Errorf("update_file not found")
	}
	defer fn.Close()
	result, err := fn.Call(operation)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ApplyPatchOperation wraps a Python ApplyPatchOperation instance.
// Represents a single apply_patch editor operation requested by the model.
type ApplyPatchOperation struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewApplyPatchOperation creates a new ApplyPatchOperation instance.
func (m *Module) NewApplyPatchOperation(type_ any, path string, diff any, ctxWrapper *RunContextWrapper) (*ApplyPatchOperation, error) {
	cls := m.mod.Attr("ApplyPatchOperation")
	if cls == nil {
		return nil, fmt.Errorf("class ApplyPatchOperation not found")
	}
	defer cls.Close()
	obj, err := cls.Call(type_, path, diff, ctxWrapper)
	if err != nil {
		return nil, err
	}
	return &ApplyPatchOperation{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ApplyPatchOperation) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ApplyPatchOperation) Object() *pyffi.Object {
	return w.obj
}

// ApplyPatchResult wraps a Python ApplyPatchResult instance.
// Optional metadata returned by editor operations.
type ApplyPatchResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewApplyPatchResult creates a new ApplyPatchResult instance.
func (m *Module) NewApplyPatchResult(status any, output any) (*ApplyPatchResult, error) {
	cls := m.mod.Attr("ApplyPatchResult")
	if cls == nil {
		return nil, fmt.Errorf("class ApplyPatchResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(status, output)
	if err != nil {
		return nil, err
	}
	return &ApplyPatchResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ApplyPatchResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ApplyPatchResult) Object() *pyffi.Object {
	return w.obj
}

// ApplyPatchTool wraps a Python ApplyPatchTool instance.
// Hosted apply_patch tool. Lets the model request file mutations via unified diffs.
type ApplyPatchTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewApplyPatchTool creates a new ApplyPatchTool instance.
func (m *Module) NewApplyPatchTool(editor *ApplyPatchEditor, name string, needsApproval any, onApproval any) (*ApplyPatchTool, error) {
	cls := m.mod.Attr("ApplyPatchTool")
	if cls == nil {
		return nil, fmt.Errorf("class ApplyPatchTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(editor, name, needsApproval, onApproval)
	if err != nil {
		return nil, err
	}
	return &ApplyPatchTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ApplyPatchTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ApplyPatchTool) Object() *pyffi.Object {
	return w.obj
}

// AsyncComputer wraps a Python AsyncComputer instance.
// A computer implemented with async operations. The Computer interface abstracts the
type AsyncComputer struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAsyncComputer creates a new AsyncComputer instance.
func (m *Module) NewAsyncComputer() (*AsyncComputer, error) {
	cls := m.mod.Attr("AsyncComputer")
	if cls == nil {
		return nil, fmt.Errorf("class AsyncComputer not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &AsyncComputer{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AsyncComputer) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AsyncComputer) Object() *pyffi.Object {
	return w.obj
}

// Click wraps AsyncComputer.click.
func (w *AsyncComputer) Click(x int64, y int64, button any) error {
	fn := w.obj.Attr("click")
	if fn == nil {
		return fmt.Errorf("click not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y, button)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// DoubleClick wraps AsyncComputer.double_click.
func (w *AsyncComputer) DoubleClick(x int64, y int64) error {
	fn := w.obj.Attr("double_click")
	if fn == nil {
		return fmt.Errorf("double_click not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Drag wraps AsyncComputer.drag.
func (w *AsyncComputer) Drag(path any) error {
	fn := w.obj.Attr("drag")
	if fn == nil {
		return fmt.Errorf("drag not found")
	}
	defer fn.Close()
	result, err := fn.Call(path)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Keypress wraps AsyncComputer.keypress.
func (w *AsyncComputer) Keypress(keys any) error {
	fn := w.obj.Attr("keypress")
	if fn == nil {
		return fmt.Errorf("keypress not found")
	}
	defer fn.Close()
	result, err := fn.Call(keys)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Move wraps AsyncComputer.move.
func (w *AsyncComputer) Move(x int64, y int64) error {
	fn := w.obj.Attr("move")
	if fn == nil {
		return fmt.Errorf("move not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Screenshot wraps AsyncComputer.screenshot.
func (w *AsyncComputer) Screenshot() (string, error) {
	fn := w.obj.Attr("screenshot")
	if fn == nil {
		return "", fmt.Errorf("screenshot not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Scroll wraps AsyncComputer.scroll.
func (w *AsyncComputer) Scroll(x int64, y int64, scrollX int64, scrollY int64) error {
	fn := w.obj.Attr("scroll")
	if fn == nil {
		return fmt.Errorf("scroll not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y, scrollX, scrollY)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Type wraps AsyncComputer.type.
func (w *AsyncComputer) Type(text string) error {
	fn := w.obj.Attr("type")
	if fn == nil {
		return fmt.Errorf("type not found")
	}
	defer fn.Close()
	result, err := fn.Call(text)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Wait wraps AsyncComputer.wait.
func (w *AsyncComputer) Wait() error {
	fn := w.obj.Attr("wait")
	if fn == nil {
		return fmt.Errorf("wait not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// AsyncOpenAI wraps a Python AsyncOpenAI instance.
// Abstract base class for generic types.
type AsyncOpenAI struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewAsyncOpenAI creates a new AsyncOpenAI instance.
func (m *Module) NewAsyncOpenAI(apiKey any, organization any, project any, webhookSecret any, baseUrl any, websocketBaseUrl any, timeout any, maxRetries int64, defaultHeaders any, defaultQuery any, httpClient any, StrictResponseValidation bool) (*AsyncOpenAI, error) {
	cls := m.mod.Attr("AsyncOpenAI")
	if cls == nil {
		return nil, fmt.Errorf("class AsyncOpenAI not found")
	}
	defer cls.Close()
	obj, err := cls.Call(apiKey, organization, project, webhookSecret, baseUrl, websocketBaseUrl, timeout, maxRetries, defaultHeaders, defaultQuery, httpClient, StrictResponseValidation)
	if err != nil {
		return nil, err
	}
	return &AsyncOpenAI{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *AsyncOpenAI) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *AsyncOpenAI) Object() *pyffi.Object {
	return w.obj
}

// Copy wraps AsyncOpenAI.copy.
// Create a new client instance re-using the same options given to the current client with optional overriding.
func (w *AsyncOpenAI) Copy(apiKey any, organization any, project any, webhookSecret any, websocketBaseUrl any, baseUrl any, timeout any, httpClient any, maxRetries any, defaultHeaders any, setDefaultHeaders any, defaultQuery any, setDefaultQuery any, ExtraKwargs any) (any, error) {
	fn := w.obj.Attr("copy")
	if fn == nil {
		return nil, fmt.Errorf("copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(apiKey, organization, project, webhookSecret, websocketBaseUrl, baseUrl, timeout, httpClient, maxRetries, defaultHeaders, setDefaultHeaders, defaultQuery, setDefaultQuery, ExtraKwargs)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Delete wraps AsyncOpenAI.delete.
func (w *AsyncOpenAI) Delete(path string, castTo any, body any, content any, options any) (any, error) {
	fn := w.obj.Attr("delete")
	if fn == nil {
		return nil, fmt.Errorf("delete not found")
	}
	defer fn.Close()
	result, err := fn.Call(path, castTo, body, content, options)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Get wraps AsyncOpenAI.get.
func (w *AsyncOpenAI) Get(path string, castTo any, options any, stream bool, streamCls any) (any, error) {
	fn := w.obj.Attr("get")
	if fn == nil {
		return nil, fmt.Errorf("get not found")
	}
	defer fn.Close()
	result, err := fn.Call(path, castTo, options, stream, streamCls)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetApiList wraps AsyncOpenAI.get_api_list.
func (w *AsyncOpenAI) GetApiList(path string, model any, page any, body any, options any, method string) (any, error) {
	fn := w.obj.Attr("get_api_list")
	if fn == nil {
		return nil, fmt.Errorf("get_api_list not found")
	}
	defer fn.Close()
	result, err := fn.Call(path, model, page, body, options, method)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// IsClosed wraps AsyncOpenAI.is_closed.
func (w *AsyncOpenAI) IsClosed() (bool, error) {
	fn := w.obj.Attr("is_closed")
	if fn == nil {
		return false, fmt.Errorf("is_closed not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return false, err
	}
	defer result.Close()
	return result.Bool()
}

// Patch wraps AsyncOpenAI.patch.
func (w *AsyncOpenAI) Patch(path string, castTo any, body any, content any, files any, options any) (any, error) {
	fn := w.obj.Attr("patch")
	if fn == nil {
		return nil, fmt.Errorf("patch not found")
	}
	defer fn.Close()
	result, err := fn.Call(path, castTo, body, content, files, options)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PlatformHeaders wraps AsyncOpenAI.platform_headers.
func (w *AsyncOpenAI) PlatformHeaders() (any, error) {
	fn := w.obj.Attr("platform_headers")
	if fn == nil {
		return nil, fmt.Errorf("platform_headers not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Post wraps AsyncOpenAI.post.
func (w *AsyncOpenAI) Post(path string, castTo any, body any, content any, files any, options any, stream bool, streamCls any) (any, error) {
	fn := w.obj.Attr("post")
	if fn == nil {
		return nil, fmt.Errorf("post not found")
	}
	defer fn.Close()
	result, err := fn.Call(path, castTo, body, content, files, options, stream, streamCls)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Put wraps AsyncOpenAI.put.
func (w *AsyncOpenAI) Put(path string, castTo any, body any, content any, files any, options any) (any, error) {
	fn := w.obj.Attr("put")
	if fn == nil {
		return nil, fmt.Errorf("put not found")
	}
	defer fn.Close()
	result, err := fn.Call(path, castTo, body, content, files, options)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Request wraps AsyncOpenAI.request.
func (w *AsyncOpenAI) Request(castTo any, options any, stream bool, streamCls any) (any, error) {
	fn := w.obj.Attr("request")
	if fn == nil {
		return nil, fmt.Errorf("request not found")
	}
	defer fn.Close()
	result, err := fn.Call(castTo, options, stream, streamCls)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// WithOptions wraps AsyncOpenAI.with_options.
// Create a new client instance re-using the same options given to the current client with optional overriding.
func (w *AsyncOpenAI) WithOptions(apiKey any, organization any, project any, webhookSecret any, websocketBaseUrl any, baseUrl any, timeout any, httpClient any, maxRetries any, defaultHeaders any, setDefaultHeaders any, defaultQuery any, setDefaultQuery any, ExtraKwargs any) (any, error) {
	fn := w.obj.Attr("with_options")
	if fn == nil {
		return nil, fmt.Errorf("with_options not found")
	}
	defer fn.Close()
	result, err := fn.Call(apiKey, organization, project, webhookSecret, websocketBaseUrl, baseUrl, timeout, httpClient, maxRetries, defaultHeaders, setDefaultHeaders, defaultQuery, setDefaultQuery, ExtraKwargs)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// CodeInterpreterTool wraps a Python CodeInterpreterTool instance.
// A tool that allows the LLM to execute code in a sandboxed environment.
type CodeInterpreterTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewCodeInterpreterTool creates a new CodeInterpreterTool instance.
func (m *Module) NewCodeInterpreterTool(toolConfig any) (*CodeInterpreterTool, error) {
	cls := m.mod.Attr("CodeInterpreterTool")
	if cls == nil {
		return nil, fmt.Errorf("class CodeInterpreterTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(toolConfig)
	if err != nil {
		return nil, err
	}
	return &CodeInterpreterTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *CodeInterpreterTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *CodeInterpreterTool) Object() *pyffi.Object {
	return w.obj
}

// CompactionItem wraps a Python CompactionItem instance.
// Represents a compaction item from responses.compact.
type CompactionItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewCompactionItem creates a new CompactionItem instance.
func (m *Module) NewCompactionItem(agent any, rawItem any, type_ any) (*CompactionItem, error) {
	cls := m.mod.Attr("CompactionItem")
	if cls == nil {
		return nil, fmt.Errorf("class CompactionItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_)
	if err != nil {
		return nil, err
	}
	return &CompactionItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *CompactionItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *CompactionItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps CompactionItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *CompactionItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps CompactionItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *CompactionItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Computer wraps a Python Computer instance.
// A computer implemented with sync operations. The Computer interface abstracts the
type Computer struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewComputer creates a new Computer instance.
func (m *Module) NewComputer() (*Computer, error) {
	cls := m.mod.Attr("Computer")
	if cls == nil {
		return nil, fmt.Errorf("class Computer not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Computer{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Computer) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Computer) Object() *pyffi.Object {
	return w.obj
}

// Click wraps Computer.click.
func (w *Computer) Click(x int64, y int64, button any) error {
	fn := w.obj.Attr("click")
	if fn == nil {
		return fmt.Errorf("click not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y, button)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// DoubleClick wraps Computer.double_click.
func (w *Computer) DoubleClick(x int64, y int64) error {
	fn := w.obj.Attr("double_click")
	if fn == nil {
		return fmt.Errorf("double_click not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Drag wraps Computer.drag.
func (w *Computer) Drag(path any) error {
	fn := w.obj.Attr("drag")
	if fn == nil {
		return fmt.Errorf("drag not found")
	}
	defer fn.Close()
	result, err := fn.Call(path)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Keypress wraps Computer.keypress.
func (w *Computer) Keypress(keys any) error {
	fn := w.obj.Attr("keypress")
	if fn == nil {
		return fmt.Errorf("keypress not found")
	}
	defer fn.Close()
	result, err := fn.Call(keys)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Move wraps Computer.move.
func (w *Computer) Move(x int64, y int64) error {
	fn := w.obj.Attr("move")
	if fn == nil {
		return fmt.Errorf("move not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Screenshot wraps Computer.screenshot.
func (w *Computer) Screenshot() (string, error) {
	fn := w.obj.Attr("screenshot")
	if fn == nil {
		return "", fmt.Errorf("screenshot not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Scroll wraps Computer.scroll.
func (w *Computer) Scroll(x int64, y int64, scrollX int64, scrollY int64) error {
	fn := w.obj.Attr("scroll")
	if fn == nil {
		return fmt.Errorf("scroll not found")
	}
	defer fn.Close()
	result, err := fn.Call(x, y, scrollX, scrollY)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Type wraps Computer.type.
func (w *Computer) Type(text string) error {
	fn := w.obj.Attr("type")
	if fn == nil {
		return fmt.Errorf("type not found")
	}
	defer fn.Close()
	result, err := fn.Call(text)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Wait wraps Computer.wait.
func (w *Computer) Wait() error {
	fn := w.obj.Attr("wait")
	if fn == nil {
		return fmt.Errorf("wait not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ComputerProvider wraps a Python ComputerProvider instance.
// Configures create/dispose hooks for per-run computer lifecycle management.
type ComputerProvider struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewComputerProvider creates a new ComputerProvider instance.
func (m *Module) NewComputerProvider(create any, dispose any) (*ComputerProvider, error) {
	cls := m.mod.Attr("ComputerProvider")
	if cls == nil {
		return nil, fmt.Errorf("class ComputerProvider not found")
	}
	defer cls.Close()
	obj, err := cls.Call(create, dispose)
	if err != nil {
		return nil, err
	}
	return &ComputerProvider{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ComputerProvider) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ComputerProvider) Object() *pyffi.Object {
	return w.obj
}

// ComputerTool wraps a Python ComputerTool instance.
// A local computer harness exposed through the Responses API computer tool.
type ComputerTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewComputerTool creates a new ComputerTool instance.
func (m *Module) NewComputerTool(computer any, onSafetyCheck any) (*ComputerTool, error) {
	cls := m.mod.Attr("ComputerTool")
	if cls == nil {
		return nil, fmt.Errorf("class ComputerTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(computer, onSafetyCheck)
	if err != nil {
		return nil, err
	}
	return &ComputerTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ComputerTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ComputerTool) Object() *pyffi.Object {
	return w.obj
}

// CustomSpanData wraps a Python CustomSpanData instance.
// Represents a Custom Span in the trace.
type CustomSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewCustomSpanData creates a new CustomSpanData instance.
func (m *Module) NewCustomSpanData(name string, data any) (*CustomSpanData, error) {
	cls := m.mod.Attr("CustomSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class CustomSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, data)
	if err != nil {
		return nil, err
	}
	return &CustomSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *CustomSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *CustomSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps CustomSpanData.export.
// Export the span data as a dictionary.
func (w *CustomSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// FileSearchTool wraps a Python FileSearchTool instance.
// A hosted tool that lets the LLM search through a vector store. Currently only supported with
type FileSearchTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewFileSearchTool creates a new FileSearchTool instance.
func (m *Module) NewFileSearchTool(vectorStoreIds any, maxNumResults any, includeSearchResults bool, rankingOptions any, filters any) (*FileSearchTool, error) {
	cls := m.mod.Attr("FileSearchTool")
	if cls == nil {
		return nil, fmt.Errorf("class FileSearchTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(vectorStoreIds, maxNumResults, includeSearchResults, rankingOptions, filters)
	if err != nil {
		return nil, err
	}
	return &FileSearchTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *FileSearchTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *FileSearchTool) Object() *pyffi.Object {
	return w.obj
}

// FunctionSpanData wraps a Python FunctionSpanData instance.
// Represents a Function Span in the trace.
type FunctionSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewFunctionSpanData creates a new FunctionSpanData instance.
func (m *Module) NewFunctionSpanData(name string, input any, output any, mcpData any) (*FunctionSpanData, error) {
	cls := m.mod.Attr("FunctionSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class FunctionSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, input, output, mcpData)
	if err != nil {
		return nil, err
	}
	return &FunctionSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *FunctionSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *FunctionSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps FunctionSpanData.export.
// Export the span data as a dictionary.
func (w *FunctionSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// FunctionTool wraps a Python FunctionTool instance.
// A tool that wraps a function. In most cases, you should use  the `function_tool` helpers to
type FunctionTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewFunctionTool creates a new FunctionTool instance.
func (m *Module) NewFunctionTool(name string, description string, paramsJsonSchema any, onInvokeTool any, strictJsonSchema bool, isEnabled any, toolInputGuardrails any, toolOutputGuardrails any, needsApproval any, timeoutSeconds any, timeoutBehavior any, timeoutErrorFunction any, deferLoading bool, FailureErrorFunction any, UseDefaultFailureErrorFunction bool, IsAgentTool bool, IsCodexTool bool, AgentInstance any, ToolNamespace any, ToolNamespaceDescription any, McpTitle any) (*FunctionTool, error) {
	cls := m.mod.Attr("FunctionTool")
	if cls == nil {
		return nil, fmt.Errorf("class FunctionTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, description, paramsJsonSchema, onInvokeTool, strictJsonSchema, isEnabled, toolInputGuardrails, toolOutputGuardrails, needsApproval, timeoutSeconds, timeoutBehavior, timeoutErrorFunction, deferLoading, FailureErrorFunction, UseDefaultFailureErrorFunction, IsAgentTool, IsCodexTool, AgentInstance, ToolNamespace, ToolNamespaceDescription, McpTitle)
	if err != nil {
		return nil, err
	}
	return &FunctionTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *FunctionTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *FunctionTool) Object() *pyffi.Object {
	return w.obj
}

// FunctionToolResult wraps a Python FunctionToolResult instance.
// FunctionToolResult(tool: 'FunctionTool', output: 'Any', run_item: 'RunItem | None', interruptions: 'list[ToolApprovalItem]' = <factory>, agent_run_result: 'Any' = None)
type FunctionToolResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewFunctionToolResult creates a new FunctionToolResult instance.
func (m *Module) NewFunctionToolResult(tool *FunctionTool, output any, runItem any, interruptions any, agentRunResult any) (*FunctionToolResult, error) {
	cls := m.mod.Attr("FunctionToolResult")
	if cls == nil {
		return nil, fmt.Errorf("class FunctionToolResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(tool, output, runItem, interruptions, agentRunResult)
	if err != nil {
		return nil, err
	}
	return &FunctionToolResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *FunctionToolResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *FunctionToolResult) Object() *pyffi.Object {
	return w.obj
}

// GenerateDynamicPromptData wraps a Python GenerateDynamicPromptData instance.
// Inputs to a function that allows you to dynamically generate a prompt.
type GenerateDynamicPromptData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewGenerateDynamicPromptData creates a new GenerateDynamicPromptData instance.
func (m *Module) NewGenerateDynamicPromptData(context any, agent any) (*GenerateDynamicPromptData, error) {
	cls := m.mod.Attr("GenerateDynamicPromptData")
	if cls == nil {
		return nil, fmt.Errorf("class GenerateDynamicPromptData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(context, agent)
	if err != nil {
		return nil, err
	}
	return &GenerateDynamicPromptData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *GenerateDynamicPromptData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *GenerateDynamicPromptData) Object() *pyffi.Object {
	return w.obj
}

// GenerationSpanData wraps a Python GenerationSpanData instance.
// Represents a Generation Span in the trace.
type GenerationSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewGenerationSpanData creates a new GenerationSpanData instance.
func (m *Module) NewGenerationSpanData(input any, output any, model any, modelConfig any, usage any) (*GenerationSpanData, error) {
	cls := m.mod.Attr("GenerationSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class GenerationSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, output, model, modelConfig, usage)
	if err != nil {
		return nil, err
	}
	return &GenerationSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *GenerationSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *GenerationSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps GenerationSpanData.export.
// Export the span data as a dictionary.
func (w *GenerationSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GuardrailFunctionOutput wraps a Python GuardrailFunctionOutput instance.
// The output of a guardrail function.
type GuardrailFunctionOutput struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewGuardrailFunctionOutput creates a new GuardrailFunctionOutput instance.
func (m *Module) NewGuardrailFunctionOutput(outputInfo any, tripwireTriggered bool) (*GuardrailFunctionOutput, error) {
	cls := m.mod.Attr("GuardrailFunctionOutput")
	if cls == nil {
		return nil, fmt.Errorf("class GuardrailFunctionOutput not found")
	}
	defer cls.Close()
	obj, err := cls.Call(outputInfo, tripwireTriggered)
	if err != nil {
		return nil, err
	}
	return &GuardrailFunctionOutput{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *GuardrailFunctionOutput) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *GuardrailFunctionOutput) Object() *pyffi.Object {
	return w.obj
}

// GuardrailSpanData wraps a Python GuardrailSpanData instance.
// Represents a Guardrail Span in the trace.
type GuardrailSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewGuardrailSpanData creates a new GuardrailSpanData instance.
func (m *Module) NewGuardrailSpanData(name string, triggered bool) (*GuardrailSpanData, error) {
	cls := m.mod.Attr("GuardrailSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class GuardrailSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, triggered)
	if err != nil {
		return nil, err
	}
	return &GuardrailSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *GuardrailSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *GuardrailSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps GuardrailSpanData.export.
// Export the span data as a dictionary.
func (w *GuardrailSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Handoff wraps a Python Handoff instance.
// A handoff is when an agent delegates a task to another agent.
type Handoff struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewHandoff creates a new Handoff instance.
func (m *Module) NewHandoff(toolName string, toolDescription string, inputJsonSchema any, onInvokeHandoff any, agentName string, inputFilter any, nestHandoffHistory any, strictJsonSchema bool, isEnabled any) (*Handoff, error) {
	cls := m.mod.Attr("Handoff")
	if cls == nil {
		return nil, fmt.Errorf("class Handoff not found")
	}
	defer cls.Close()
	obj, err := cls.Call(toolName, toolDescription, inputJsonSchema, onInvokeHandoff, agentName, inputFilter, nestHandoffHistory, strictJsonSchema, isEnabled)
	if err != nil {
		return nil, err
	}
	return &Handoff{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Handoff) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Handoff) Object() *pyffi.Object {
	return w.obj
}

// GetTransferMessage wraps Handoff.get_transfer_message.
func (w *Handoff) GetTransferMessage(agent any) (string, error) {
	fn := w.obj.Attr("get_transfer_message")
	if fn == nil {
		return "", fmt.Errorf("get_transfer_message not found")
	}
	defer fn.Close()
	result, err := fn.Call(agent)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// HandoffCallItem wraps a Python HandoffCallItem instance.
// Represents a tool call for a handoff from one agent to another.
type HandoffCallItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewHandoffCallItem creates a new HandoffCallItem instance.
func (m *Module) NewHandoffCallItem(agent any, rawItem any, type_ any) (*HandoffCallItem, error) {
	cls := m.mod.Attr("HandoffCallItem")
	if cls == nil {
		return nil, fmt.Errorf("class HandoffCallItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_)
	if err != nil {
		return nil, err
	}
	return &HandoffCallItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *HandoffCallItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *HandoffCallItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps HandoffCallItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *HandoffCallItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps HandoffCallItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *HandoffCallItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// HandoffInputData wraps a Python HandoffInputData instance.
// HandoffInputData(input_history: 'str | tuple[TResponseInputItem, ...]', pre_handoff_items: 'tuple[RunItem, ...]', new_items: 'tuple[RunItem, ...]', run_context: 'RunContextWrapper[Any] | None' = None, input_items: 'tuple[RunItem, ...] | None' = None)
type HandoffInputData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewHandoffInputData creates a new HandoffInputData instance.
func (m *Module) NewHandoffInputData(inputHistory any, preHandoffItems any, newItems any, runContext any, inputItems any) (*HandoffInputData, error) {
	cls := m.mod.Attr("HandoffInputData")
	if cls == nil {
		return nil, fmt.Errorf("class HandoffInputData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(inputHistory, preHandoffItems, newItems, runContext, inputItems)
	if err != nil {
		return nil, err
	}
	return &HandoffInputData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *HandoffInputData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *HandoffInputData) Object() *pyffi.Object {
	return w.obj
}

// Clone wraps HandoffInputData.clone.
// Make a copy of the handoff input data, with the given arguments changed. For example, you
func (w *HandoffInputData) Clone() (*HandoffInputData, error) {
	fn := w.obj.Attr("clone")
	if fn == nil {
		return nil, fmt.Errorf("clone not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	return &HandoffInputData{obj: result, rt: w.rt}, nil
}

// HandoffOutputItem wraps a Python HandoffOutputItem instance.
// Represents the output of a handoff.
type HandoffOutputItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewHandoffOutputItem creates a new HandoffOutputItem instance.
func (m *Module) NewHandoffOutputItem(agent any, rawItem any, sourceAgent any, targetAgent any, type_ any) (*HandoffOutputItem, error) {
	cls := m.mod.Attr("HandoffOutputItem")
	if cls == nil {
		return nil, fmt.Errorf("class HandoffOutputItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, sourceAgent, targetAgent, type_)
	if err != nil {
		return nil, err
	}
	return &HandoffOutputItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *HandoffOutputItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *HandoffOutputItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps HandoffOutputItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *HandoffOutputItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps HandoffOutputItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *HandoffOutputItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// HandoffSpanData wraps a Python HandoffSpanData instance.
// Represents a Handoff Span in the trace.
type HandoffSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewHandoffSpanData creates a new HandoffSpanData instance.
func (m *Module) NewHandoffSpanData(fromAgent any, toAgent any) (*HandoffSpanData, error) {
	cls := m.mod.Attr("HandoffSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class HandoffSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(fromAgent, toAgent)
	if err != nil {
		return nil, err
	}
	return &HandoffSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *HandoffSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *HandoffSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps HandoffSpanData.export.
// Export the span data as a dictionary.
func (w *HandoffSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// HostedMCPTool wraps a Python HostedMCPTool instance.
// A tool that allows the LLM to use a remote MCP server. The LLM will automatically list and
type HostedMCPTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewHostedMCPTool creates a new HostedMCPTool instance.
func (m *Module) NewHostedMCPTool(toolConfig any, onApprovalRequest any) (*HostedMCPTool, error) {
	cls := m.mod.Attr("HostedMCPTool")
	if cls == nil {
		return nil, fmt.Errorf("class HostedMCPTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(toolConfig, onApprovalRequest)
	if err != nil {
		return nil, err
	}
	return &HostedMCPTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *HostedMCPTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *HostedMCPTool) Object() *pyffi.Object {
	return w.obj
}

// ImageGenerationTool wraps a Python ImageGenerationTool instance.
// A tool that allows the LLM to generate images.
type ImageGenerationTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewImageGenerationTool creates a new ImageGenerationTool instance.
func (m *Module) NewImageGenerationTool(toolConfig any) (*ImageGenerationTool, error) {
	cls := m.mod.Attr("ImageGenerationTool")
	if cls == nil {
		return nil, fmt.Errorf("class ImageGenerationTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(toolConfig)
	if err != nil {
		return nil, err
	}
	return &ImageGenerationTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ImageGenerationTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ImageGenerationTool) Object() *pyffi.Object {
	return w.obj
}

// InputGuardrail wraps a Python InputGuardrail instance.
// Input guardrails are checks that run either in parallel with the agent or before it starts.
type InputGuardrail struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewInputGuardrail creates a new InputGuardrail instance.
func (m *Module) NewInputGuardrail(guardrailFunction any, name any, runInParallel bool) (*InputGuardrail, error) {
	cls := m.mod.Attr("InputGuardrail")
	if cls == nil {
		return nil, fmt.Errorf("class InputGuardrail not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrailFunction, name, runInParallel)
	if err != nil {
		return nil, err
	}
	return &InputGuardrail{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *InputGuardrail) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *InputGuardrail) Object() *pyffi.Object {
	return w.obj
}

// GetName wraps InputGuardrail.get_name.
func (w *InputGuardrail) GetName() (string, error) {
	fn := w.obj.Attr("get_name")
	if fn == nil {
		return "", fmt.Errorf("get_name not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Run wraps InputGuardrail.run.
func (w *InputGuardrail) Run(agent any, input any, context any) (*InputGuardrailResult, error) {
	fn := w.obj.Attr("run")
	if fn == nil {
		return nil, fmt.Errorf("run not found")
	}
	defer fn.Close()
	result, err := fn.Call(agent, input, context)
	if err != nil {
		return nil, err
	}
	return &InputGuardrailResult{obj: result, rt: w.rt}, nil
}

// InputGuardrailResult wraps a Python InputGuardrailResult instance.
// The result of a guardrail run.
type InputGuardrailResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewInputGuardrailResult creates a new InputGuardrailResult instance.
func (m *Module) NewInputGuardrailResult(guardrail any, output *GuardrailFunctionOutput) (*InputGuardrailResult, error) {
	cls := m.mod.Attr("InputGuardrailResult")
	if cls == nil {
		return nil, fmt.Errorf("class InputGuardrailResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrail, output)
	if err != nil {
		return nil, err
	}
	return &InputGuardrailResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *InputGuardrailResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *InputGuardrailResult) Object() *pyffi.Object {
	return w.obj
}

// InputGuardrailTripwireTriggered wraps a Python InputGuardrailTripwireTriggered instance.
// Exception raised when a guardrail tripwire is triggered.
type InputGuardrailTripwireTriggered struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewInputGuardrailTripwireTriggered creates a new InputGuardrailTripwireTriggered instance.
func (m *Module) NewInputGuardrailTripwireTriggered(guardrailResult *InputGuardrailResult) (*InputGuardrailTripwireTriggered, error) {
	cls := m.mod.Attr("InputGuardrailTripwireTriggered")
	if cls == nil {
		return nil, fmt.Errorf("class InputGuardrailTripwireTriggered not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrailResult)
	if err != nil {
		return nil, err
	}
	return &InputGuardrailTripwireTriggered{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *InputGuardrailTripwireTriggered) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *InputGuardrailTripwireTriggered) Object() *pyffi.Object {
	return w.obj
}

// ItemHelpers wraps a Python ItemHelpers instance.
type ItemHelpers struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewItemHelpers creates a new ItemHelpers instance.
func (m *Module) NewItemHelpers() (*ItemHelpers, error) {
	cls := m.mod.Attr("ItemHelpers")
	if cls == nil {
		return nil, fmt.Errorf("class ItemHelpers not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ItemHelpers{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ItemHelpers) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ItemHelpers) Object() *pyffi.Object {
	return w.obj
}

// LocalShellCommandRequest wraps a Python LocalShellCommandRequest instance.
// A request to execute a command on a shell.
type LocalShellCommandRequest struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewLocalShellCommandRequest creates a new LocalShellCommandRequest instance.
func (m *Module) NewLocalShellCommandRequest(ctxWrapper any, data any) (*LocalShellCommandRequest, error) {
	cls := m.mod.Attr("LocalShellCommandRequest")
	if cls == nil {
		return nil, fmt.Errorf("class LocalShellCommandRequest not found")
	}
	defer cls.Close()
	obj, err := cls.Call(ctxWrapper, data)
	if err != nil {
		return nil, err
	}
	return &LocalShellCommandRequest{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *LocalShellCommandRequest) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *LocalShellCommandRequest) Object() *pyffi.Object {
	return w.obj
}

// LocalShellTool wraps a Python LocalShellTool instance.
// A tool that allows the LLM to execute commands on a shell.
type LocalShellTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewLocalShellTool creates a new LocalShellTool instance.
func (m *Module) NewLocalShellTool(executor any) (*LocalShellTool, error) {
	cls := m.mod.Attr("LocalShellTool")
	if cls == nil {
		return nil, fmt.Errorf("class LocalShellTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(executor)
	if err != nil {
		return nil, err
	}
	return &LocalShellTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *LocalShellTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *LocalShellTool) Object() *pyffi.Object {
	return w.obj
}

// MCPApprovalRequestItem wraps a Python MCPApprovalRequestItem instance.
// Represents a request for MCP approval.
type MCPApprovalRequestItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMCPApprovalRequestItem creates a new MCPApprovalRequestItem instance.
func (m *Module) NewMCPApprovalRequestItem(agent any, rawItem any, type_ any) (*MCPApprovalRequestItem, error) {
	cls := m.mod.Attr("MCPApprovalRequestItem")
	if cls == nil {
		return nil, fmt.Errorf("class MCPApprovalRequestItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_)
	if err != nil {
		return nil, err
	}
	return &MCPApprovalRequestItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MCPApprovalRequestItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MCPApprovalRequestItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps MCPApprovalRequestItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *MCPApprovalRequestItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps MCPApprovalRequestItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *MCPApprovalRequestItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// MCPApprovalResponseItem wraps a Python MCPApprovalResponseItem instance.
// Represents a response to an MCP approval request.
type MCPApprovalResponseItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMCPApprovalResponseItem creates a new MCPApprovalResponseItem instance.
func (m *Module) NewMCPApprovalResponseItem(agent any, rawItem any, type_ any) (*MCPApprovalResponseItem, error) {
	cls := m.mod.Attr("MCPApprovalResponseItem")
	if cls == nil {
		return nil, fmt.Errorf("class MCPApprovalResponseItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_)
	if err != nil {
		return nil, err
	}
	return &MCPApprovalResponseItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MCPApprovalResponseItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MCPApprovalResponseItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps MCPApprovalResponseItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *MCPApprovalResponseItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps MCPApprovalResponseItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *MCPApprovalResponseItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// MCPListToolsSpanData wraps a Python MCPListToolsSpanData instance.
// Represents an MCP List Tools Span in the trace.
type MCPListToolsSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMCPListToolsSpanData creates a new MCPListToolsSpanData instance.
func (m *Module) NewMCPListToolsSpanData(server any, result any) (*MCPListToolsSpanData, error) {
	cls := m.mod.Attr("MCPListToolsSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class MCPListToolsSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(server, result)
	if err != nil {
		return nil, err
	}
	return &MCPListToolsSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MCPListToolsSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MCPListToolsSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps MCPListToolsSpanData.export.
// Export the span data as a dictionary.
func (w *MCPListToolsSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// MCPToolApprovalFunctionResult wraps a Python MCPToolApprovalFunctionResult instance.
// The result of an MCP tool approval function.
type MCPToolApprovalFunctionResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMCPToolApprovalFunctionResult creates a new MCPToolApprovalFunctionResult instance.
func (m *Module) NewMCPToolApprovalFunctionResult() (*MCPToolApprovalFunctionResult, error) {
	cls := m.mod.Attr("MCPToolApprovalFunctionResult")
	if cls == nil {
		return nil, fmt.Errorf("class MCPToolApprovalFunctionResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &MCPToolApprovalFunctionResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MCPToolApprovalFunctionResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MCPToolApprovalFunctionResult) Object() *pyffi.Object {
	return w.obj
}

// MCPToolApprovalRequest wraps a Python MCPToolApprovalRequest instance.
// A request to approve a tool call.
type MCPToolApprovalRequest struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMCPToolApprovalRequest creates a new MCPToolApprovalRequest instance.
func (m *Module) NewMCPToolApprovalRequest(ctxWrapper any, data any) (*MCPToolApprovalRequest, error) {
	cls := m.mod.Attr("MCPToolApprovalRequest")
	if cls == nil {
		return nil, fmt.Errorf("class MCPToolApprovalRequest not found")
	}
	defer cls.Close()
	obj, err := cls.Call(ctxWrapper, data)
	if err != nil {
		return nil, err
	}
	return &MCPToolApprovalRequest{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MCPToolApprovalRequest) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MCPToolApprovalRequest) Object() *pyffi.Object {
	return w.obj
}

// MaxTurnsExceeded wraps a Python MaxTurnsExceeded instance.
// Exception raised when the maximum number of turns is exceeded.
type MaxTurnsExceeded struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMaxTurnsExceeded creates a new MaxTurnsExceeded instance.
func (m *Module) NewMaxTurnsExceeded(message string) (*MaxTurnsExceeded, error) {
	cls := m.mod.Attr("MaxTurnsExceeded")
	if cls == nil {
		return nil, fmt.Errorf("class MaxTurnsExceeded not found")
	}
	defer cls.Close()
	obj, err := cls.Call(message)
	if err != nil {
		return nil, err
	}
	return &MaxTurnsExceeded{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MaxTurnsExceeded) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MaxTurnsExceeded) Object() *pyffi.Object {
	return w.obj
}

// MessageOutputItem wraps a Python MessageOutputItem instance.
// Represents a message from the LLM.
type MessageOutputItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMessageOutputItem creates a new MessageOutputItem instance.
func (m *Module) NewMessageOutputItem(agent any, rawItem any, type_ any) (*MessageOutputItem, error) {
	cls := m.mod.Attr("MessageOutputItem")
	if cls == nil {
		return nil, fmt.Errorf("class MessageOutputItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_)
	if err != nil {
		return nil, err
	}
	return &MessageOutputItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MessageOutputItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MessageOutputItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps MessageOutputItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *MessageOutputItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps MessageOutputItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *MessageOutputItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Model wraps a Python Model instance.
// The base interface for calling an LLM.
type Model struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModel creates a new Model instance.
func (m *Module) NewModel() (*Model, error) {
	cls := m.mod.Attr("Model")
	if cls == nil {
		return nil, fmt.Errorf("class Model not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Model{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Model) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Model) Object() *pyffi.Object {
	return w.obj
}

// GetResponse wraps Model.get_response.
// Get a response from the model.
func (w *Model) GetResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (*ModelResponse, error) {
	fn := w.obj.Attr("get_response")
	if fn == nil {
		return nil, fmt.Errorf("get_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	return &ModelResponse{obj: result, rt: w.rt}, nil
}

// GetRetryAdvice wraps Model.get_retry_advice.
// Return provider-specific retry guidance for a failed model request.
func (w *Model) GetRetryAdvice(request *ModelRetryAdviceRequest) (*ModelRetryAdvice, error) {
	fn := w.obj.Attr("get_retry_advice")
	if fn == nil {
		return nil, fmt.Errorf("get_retry_advice not found")
	}
	defer fn.Close()
	result, err := fn.Call(request)
	if err != nil {
		return nil, err
	}
	return &ModelRetryAdvice{obj: result, rt: w.rt}, nil
}

// StreamResponse wraps Model.stream_response.
// Stream a response from the model.
func (w *Model) StreamResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (any, error) {
	fn := w.obj.Attr("stream_response")
	if fn == nil {
		return nil, fmt.Errorf("stream_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelBehaviorError wraps a Python ModelBehaviorError instance.
// Exception raised when the model does something unexpected, e.g. calling a tool that doesn't
type ModelBehaviorError struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelBehaviorError creates a new ModelBehaviorError instance.
func (m *Module) NewModelBehaviorError(message string) (*ModelBehaviorError, error) {
	cls := m.mod.Attr("ModelBehaviorError")
	if cls == nil {
		return nil, fmt.Errorf("class ModelBehaviorError not found")
	}
	defer cls.Close()
	obj, err := cls.Call(message)
	if err != nil {
		return nil, err
	}
	return &ModelBehaviorError{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelBehaviorError) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelBehaviorError) Object() *pyffi.Object {
	return w.obj
}

// ModelProvider wraps a Python ModelProvider instance.
// The base interface for a model provider.
type ModelProvider struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelProvider creates a new ModelProvider instance.
func (m *Module) NewModelProvider() (*ModelProvider, error) {
	cls := m.mod.Attr("ModelProvider")
	if cls == nil {
		return nil, fmt.Errorf("class ModelProvider not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ModelProvider{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelProvider) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelProvider) Object() *pyffi.Object {
	return w.obj
}

// Aclose wraps ModelProvider.aclose.
// Release any resources held by the provider.
func (w *ModelProvider) Aclose() error {
	fn := w.obj.Attr("aclose")
	if fn == nil {
		return fmt.Errorf("aclose not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetModel wraps ModelProvider.get_model.
// Get a model by name.
func (w *ModelProvider) GetModel(modelName any) (*Model, error) {
	fn := w.obj.Attr("get_model")
	if fn == nil {
		return nil, fmt.Errorf("get_model not found")
	}
	defer fn.Close()
	result, err := fn.Call(modelName)
	if err != nil {
		return nil, err
	}
	return &Model{obj: result, rt: w.rt}, nil
}

// ModelResponse wraps a Python ModelResponse instance.
type ModelResponse struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelResponse creates a new ModelResponse instance.
func (m *Module) NewModelResponse(DataclassSelf any) (*ModelResponse, error) {
	cls := m.mod.Attr("ModelResponse")
	if cls == nil {
		return nil, fmt.Errorf("class ModelResponse not found")
	}
	defer cls.Close()
	obj, err := cls.Call(DataclassSelf)
	if err != nil {
		return nil, err
	}
	return &ModelResponse{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelResponse) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelResponse) Object() *pyffi.Object {
	return w.obj
}

// ToInputItems wraps ModelResponse.to_input_items.
// Convert the output into a list of input items suitable for passing to the model.
func (w *ModelResponse) ToInputItems() (any, error) {
	fn := w.obj.Attr("to_input_items")
	if fn == nil {
		return nil, fmt.Errorf("to_input_items not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelRetryAdvice wraps a Python ModelRetryAdvice instance.
// Provider-specific retry guidance returned by model adapters.
type ModelRetryAdvice struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelRetryAdvice creates a new ModelRetryAdvice instance.
func (m *Module) NewModelRetryAdvice(suggested any, retryAfter any, replaySafety any, reason any, normalized *ModelRetryNormalizedError) (*ModelRetryAdvice, error) {
	cls := m.mod.Attr("ModelRetryAdvice")
	if cls == nil {
		return nil, fmt.Errorf("class ModelRetryAdvice not found")
	}
	defer cls.Close()
	obj, err := cls.Call(suggested, retryAfter, replaySafety, reason, normalized)
	if err != nil {
		return nil, err
	}
	return &ModelRetryAdvice{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelRetryAdvice) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelRetryAdvice) Object() *pyffi.Object {
	return w.obj
}

// ModelRetryAdviceRequest wraps a Python ModelRetryAdviceRequest instance.
// Context passed to a model adapter when deriving retry advice.
type ModelRetryAdviceRequest struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelRetryAdviceRequest creates a new ModelRetryAdviceRequest instance.
func (m *Module) NewModelRetryAdviceRequest(error any, attempt int64, stream bool, previousResponseId any, conversationId any) (*ModelRetryAdviceRequest, error) {
	cls := m.mod.Attr("ModelRetryAdviceRequest")
	if cls == nil {
		return nil, fmt.Errorf("class ModelRetryAdviceRequest not found")
	}
	defer cls.Close()
	obj, err := cls.Call(error, attempt, stream, previousResponseId, conversationId)
	if err != nil {
		return nil, err
	}
	return &ModelRetryAdviceRequest{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelRetryAdviceRequest) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelRetryAdviceRequest) Object() *pyffi.Object {
	return w.obj
}

// ModelRetryBackoffSettings wraps a Python ModelRetryBackoffSettings instance.
// Backoff configuration for runner-managed model retries.
type ModelRetryBackoffSettings struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelRetryBackoffSettings creates a new ModelRetryBackoffSettings instance.
func (m *Module) NewModelRetryBackoffSettings(DataclassSelf any) (*ModelRetryBackoffSettings, error) {
	cls := m.mod.Attr("ModelRetryBackoffSettings")
	if cls == nil {
		return nil, fmt.Errorf("class ModelRetryBackoffSettings not found")
	}
	defer cls.Close()
	obj, err := cls.Call(DataclassSelf)
	if err != nil {
		return nil, err
	}
	return &ModelRetryBackoffSettings{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelRetryBackoffSettings) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelRetryBackoffSettings) Object() *pyffi.Object {
	return w.obj
}

// ToJsonDict wraps ModelRetryBackoffSettings.to_json_dict.
func (w *ModelRetryBackoffSettings) ToJsonDict() (any, error) {
	fn := w.obj.Attr("to_json_dict")
	if fn == nil {
		return nil, fmt.Errorf("to_json_dict not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelRetryNormalizedError wraps a Python ModelRetryNormalizedError instance.
// Normalized error facts exposed to retry policies.
type ModelRetryNormalizedError struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelRetryNormalizedError creates a new ModelRetryNormalizedError instance.
func (m *Module) NewModelRetryNormalizedError(statusCode any, errorCode any, message any, requestId any, retryAfter any, isAbort bool, isNetworkError bool, isTimeout bool) (*ModelRetryNormalizedError, error) {
	cls := m.mod.Attr("ModelRetryNormalizedError")
	if cls == nil {
		return nil, fmt.Errorf("class ModelRetryNormalizedError not found")
	}
	defer cls.Close()
	obj, err := cls.Call(statusCode, errorCode, message, requestId, retryAfter, isAbort, isNetworkError, isTimeout)
	if err != nil {
		return nil, err
	}
	return &ModelRetryNormalizedError{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelRetryNormalizedError) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelRetryNormalizedError) Object() *pyffi.Object {
	return w.obj
}

// ModelRetrySettings wraps a Python ModelRetrySettings instance.
// Opt-in runner-managed retry settings for model calls.
type ModelRetrySettings struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelRetrySettings creates a new ModelRetrySettings instance.
func (m *Module) NewModelRetrySettings(DataclassSelf any) (*ModelRetrySettings, error) {
	cls := m.mod.Attr("ModelRetrySettings")
	if cls == nil {
		return nil, fmt.Errorf("class ModelRetrySettings not found")
	}
	defer cls.Close()
	obj, err := cls.Call(DataclassSelf)
	if err != nil {
		return nil, err
	}
	return &ModelRetrySettings{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelRetrySettings) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelRetrySettings) Object() *pyffi.Object {
	return w.obj
}

// ToJsonDict wraps ModelRetrySettings.to_json_dict.
func (w *ModelRetrySettings) ToJsonDict() (any, error) {
	fn := w.obj.Attr("to_json_dict")
	if fn == nil {
		return nil, fmt.Errorf("to_json_dict not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelSettings wraps a Python ModelSettings instance.
// Settings to use when calling an LLM.
type ModelSettings struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewModelSettings creates a new ModelSettings instance.
func (m *Module) NewModelSettings(DataclassSelf any) (*ModelSettings, error) {
	cls := m.mod.Attr("ModelSettings")
	if cls == nil {
		return nil, fmt.Errorf("class ModelSettings not found")
	}
	defer cls.Close()
	obj, err := cls.Call(DataclassSelf)
	if err != nil {
		return nil, err
	}
	return &ModelSettings{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ModelSettings) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ModelSettings) Object() *pyffi.Object {
	return w.obj
}

// Resolve wraps ModelSettings.resolve.
// Produce a new ModelSettings by overlaying any non-None values from the
func (w *ModelSettings) Resolve(override *ModelSettings) (*ModelSettings, error) {
	fn := w.obj.Attr("resolve")
	if fn == nil {
		return nil, fmt.Errorf("resolve not found")
	}
	defer fn.Close()
	result, err := fn.Call(override)
	if err != nil {
		return nil, err
	}
	return &ModelSettings{obj: result, rt: w.rt}, nil
}

// ToJsonDict wraps ModelSettings.to_json_dict.
func (w *ModelSettings) ToJsonDict() (any, error) {
	fn := w.obj.Attr("to_json_dict")
	if fn == nil {
		return nil, fmt.Errorf("to_json_dict not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// MultiProvider wraps a Python MultiProvider instance.
// This ModelProvider maps to a Model based on the prefix of the model name. By default, the
type MultiProvider struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewMultiProvider creates a new MultiProvider instance.
func (m *Module) NewMultiProvider(providerMap any, openaiApiKey any, openaiBaseUrl any, openaiClient *AsyncOpenAI, openaiOrganization any, openaiProject any, openaiUseResponses any, openaiUseResponsesWebsocket any, openaiWebsocketBaseUrl any, openaiPrefixMode any, unknownPrefixMode any) (*MultiProvider, error) {
	cls := m.mod.Attr("MultiProvider")
	if cls == nil {
		return nil, fmt.Errorf("class MultiProvider not found")
	}
	defer cls.Close()
	obj, err := cls.Call(providerMap, openaiApiKey, openaiBaseUrl, openaiClient, openaiOrganization, openaiProject, openaiUseResponses, openaiUseResponsesWebsocket, openaiWebsocketBaseUrl, openaiPrefixMode, unknownPrefixMode)
	if err != nil {
		return nil, err
	}
	return &MultiProvider{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *MultiProvider) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *MultiProvider) Object() *pyffi.Object {
	return w.obj
}

// Aclose wraps MultiProvider.aclose.
// Close cached resources held by child providers.
func (w *MultiProvider) Aclose() error {
	fn := w.obj.Attr("aclose")
	if fn == nil {
		return fmt.Errorf("aclose not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetModel wraps MultiProvider.get_model.
// Returns a Model based on the model name. The model name can have a prefix, ending with
func (w *MultiProvider) GetModel(modelName any) (*Model, error) {
	fn := w.obj.Attr("get_model")
	if fn == nil {
		return nil, fmt.Errorf("get_model not found")
	}
	defer fn.Close()
	result, err := fn.Call(modelName)
	if err != nil {
		return nil, err
	}
	return &Model{obj: result, rt: w.rt}, nil
}

// OpenAIChatCompletionsModel wraps a Python OpenAIChatCompletionsModel instance.
// The base interface for calling an LLM.
type OpenAIChatCompletionsModel struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIChatCompletionsModel creates a new OpenAIChatCompletionsModel instance.
func (m *Module) NewOpenAIChatCompletionsModel(model any, openaiClient *AsyncOpenAI) (*OpenAIChatCompletionsModel, error) {
	cls := m.mod.Attr("OpenAIChatCompletionsModel")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIChatCompletionsModel not found")
	}
	defer cls.Close()
	obj, err := cls.Call(model, openaiClient)
	if err != nil {
		return nil, err
	}
	return &OpenAIChatCompletionsModel{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIChatCompletionsModel) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIChatCompletionsModel) Object() *pyffi.Object {
	return w.obj
}

// GetResponse wraps OpenAIChatCompletionsModel.get_response.
// Get a response from the model.
func (w *OpenAIChatCompletionsModel) GetResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (*ModelResponse, error) {
	fn := w.obj.Attr("get_response")
	if fn == nil {
		return nil, fmt.Errorf("get_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	return &ModelResponse{obj: result, rt: w.rt}, nil
}

// GetRetryAdvice wraps OpenAIChatCompletionsModel.get_retry_advice.
// Return provider-specific retry guidance for a failed model request.
func (w *OpenAIChatCompletionsModel) GetRetryAdvice(request *ModelRetryAdviceRequest) (*ModelRetryAdvice, error) {
	fn := w.obj.Attr("get_retry_advice")
	if fn == nil {
		return nil, fmt.Errorf("get_retry_advice not found")
	}
	defer fn.Close()
	result, err := fn.Call(request)
	if err != nil {
		return nil, err
	}
	return &ModelRetryAdvice{obj: result, rt: w.rt}, nil
}

// StreamResponse wraps OpenAIChatCompletionsModel.stream_response.
// Yields a partial message as it is generated, as well as the usage information.
func (w *OpenAIChatCompletionsModel) StreamResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (any, error) {
	fn := w.obj.Attr("stream_response")
	if fn == nil {
		return nil, fmt.Errorf("stream_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// OpenAIConversationsSession wraps a Python OpenAIConversationsSession instance.
// Abstract base class for session implementations.
type OpenAIConversationsSession struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIConversationsSession creates a new OpenAIConversationsSession instance.
func (m *Module) NewOpenAIConversationsSession(conversationId any, openaiClient *AsyncOpenAI, sessionSettings *SessionSettings) (*OpenAIConversationsSession, error) {
	cls := m.mod.Attr("OpenAIConversationsSession")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIConversationsSession not found")
	}
	defer cls.Close()
	obj, err := cls.Call(conversationId, openaiClient, sessionSettings)
	if err != nil {
		return nil, err
	}
	return &OpenAIConversationsSession{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIConversationsSession) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIConversationsSession) Object() *pyffi.Object {
	return w.obj
}

// AddItems wraps OpenAIConversationsSession.add_items.
// Add new items to the conversation history.
func (w *OpenAIConversationsSession) AddItems(items any) error {
	fn := w.obj.Attr("add_items")
	if fn == nil {
		return fmt.Errorf("add_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(items)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ClearSession wraps OpenAIConversationsSession.clear_session.
// Clear all items for this session.
func (w *OpenAIConversationsSession) ClearSession() error {
	fn := w.obj.Attr("clear_session")
	if fn == nil {
		return fmt.Errorf("clear_session not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetItems wraps OpenAIConversationsSession.get_items.
// Retrieve the conversation history for this session.
func (w *OpenAIConversationsSession) GetItems(limit any) (any, error) {
	fn := w.obj.Attr("get_items")
	if fn == nil {
		return nil, fmt.Errorf("get_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(limit)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PopItem wraps OpenAIConversationsSession.pop_item.
// Remove and return the most recent item from the session.
func (w *OpenAIConversationsSession) PopItem() (any, error) {
	fn := w.obj.Attr("pop_item")
	if fn == nil {
		return nil, fmt.Errorf("pop_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// OpenAIProvider wraps a Python OpenAIProvider instance.
// The base interface for a model provider.
type OpenAIProvider struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIProvider creates a new OpenAIProvider instance.
func (m *Module) NewOpenAIProvider(apiKey any, baseUrl any, websocketBaseUrl any, openaiClient *AsyncOpenAI, organization any, project any, useResponses any, useResponsesWebsocket any) (*OpenAIProvider, error) {
	cls := m.mod.Attr("OpenAIProvider")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIProvider not found")
	}
	defer cls.Close()
	obj, err := cls.Call(apiKey, baseUrl, websocketBaseUrl, openaiClient, organization, project, useResponses, useResponsesWebsocket)
	if err != nil {
		return nil, err
	}
	return &OpenAIProvider{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIProvider) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIProvider) Object() *pyffi.Object {
	return w.obj
}

// Aclose wraps OpenAIProvider.aclose.
// Close any cached model resources held by this provider.
func (w *OpenAIProvider) Aclose() error {
	fn := w.obj.Attr("aclose")
	if fn == nil {
		return fmt.Errorf("aclose not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetModel wraps OpenAIProvider.get_model.
// Get a model by name.
func (w *OpenAIProvider) GetModel(modelName any) (*Model, error) {
	fn := w.obj.Attr("get_model")
	if fn == nil {
		return nil, fmt.Errorf("get_model not found")
	}
	defer fn.Close()
	result, err := fn.Call(modelName)
	if err != nil {
		return nil, err
	}
	return &Model{obj: result, rt: w.rt}, nil
}

// OpenAIResponsesCompactionArgs wraps a Python OpenAIResponsesCompactionArgs instance.
// Arguments for the run_compaction method.
type OpenAIResponsesCompactionArgs struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIResponsesCompactionArgs creates a new OpenAIResponsesCompactionArgs instance.
func (m *Module) NewOpenAIResponsesCompactionArgs() (*OpenAIResponsesCompactionArgs, error) {
	cls := m.mod.Attr("OpenAIResponsesCompactionArgs")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIResponsesCompactionArgs not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &OpenAIResponsesCompactionArgs{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIResponsesCompactionArgs) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIResponsesCompactionArgs) Object() *pyffi.Object {
	return w.obj
}

// OpenAIResponsesCompactionAwareSession wraps a Python OpenAIResponsesCompactionAwareSession instance.
// Protocol for session implementations that support responses compaction.
type OpenAIResponsesCompactionAwareSession struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIResponsesCompactionAwareSession creates a new OpenAIResponsesCompactionAwareSession instance.
func (m *Module) NewOpenAIResponsesCompactionAwareSession() (*OpenAIResponsesCompactionAwareSession, error) {
	cls := m.mod.Attr("OpenAIResponsesCompactionAwareSession")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIResponsesCompactionAwareSession not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &OpenAIResponsesCompactionAwareSession{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIResponsesCompactionAwareSession) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIResponsesCompactionAwareSession) Object() *pyffi.Object {
	return w.obj
}

// AddItems wraps OpenAIResponsesCompactionAwareSession.add_items.
// Add new items to the conversation history.
func (w *OpenAIResponsesCompactionAwareSession) AddItems(items any) error {
	fn := w.obj.Attr("add_items")
	if fn == nil {
		return fmt.Errorf("add_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(items)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ClearSession wraps OpenAIResponsesCompactionAwareSession.clear_session.
// Clear all items for this session.
func (w *OpenAIResponsesCompactionAwareSession) ClearSession() error {
	fn := w.obj.Attr("clear_session")
	if fn == nil {
		return fmt.Errorf("clear_session not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetItems wraps OpenAIResponsesCompactionAwareSession.get_items.
// Retrieve the conversation history for this session.
func (w *OpenAIResponsesCompactionAwareSession) GetItems(limit any) (any, error) {
	fn := w.obj.Attr("get_items")
	if fn == nil {
		return nil, fmt.Errorf("get_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(limit)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PopItem wraps OpenAIResponsesCompactionAwareSession.pop_item.
// Remove and return the most recent item from the session.
func (w *OpenAIResponsesCompactionAwareSession) PopItem() (any, error) {
	fn := w.obj.Attr("pop_item")
	if fn == nil {
		return nil, fmt.Errorf("pop_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunCompaction wraps OpenAIResponsesCompactionAwareSession.run_compaction.
// Run the compaction process for the session.
func (w *OpenAIResponsesCompactionAwareSession) RunCompaction(args *OpenAIResponsesCompactionArgs) error {
	fn := w.obj.Attr("run_compaction")
	if fn == nil {
		return fmt.Errorf("run_compaction not found")
	}
	defer fn.Close()
	result, err := fn.Call(args)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// OpenAIResponsesCompactionSession wraps a Python OpenAIResponsesCompactionSession instance.
// Session decorator that triggers responses.compact when stored history grows.
type OpenAIResponsesCompactionSession struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIResponsesCompactionSession creates a new OpenAIResponsesCompactionSession instance.
func (m *Module) NewOpenAIResponsesCompactionSession(sessionId string, underlyingSession *Session, client *AsyncOpenAI, model string, compactionMode any, shouldTriggerCompaction any) (*OpenAIResponsesCompactionSession, error) {
	cls := m.mod.Attr("OpenAIResponsesCompactionSession")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIResponsesCompactionSession not found")
	}
	defer cls.Close()
	obj, err := cls.Call(sessionId, underlyingSession, client, model, compactionMode, shouldTriggerCompaction)
	if err != nil {
		return nil, err
	}
	return &OpenAIResponsesCompactionSession{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIResponsesCompactionSession) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIResponsesCompactionSession) Object() *pyffi.Object {
	return w.obj
}

// AddItems wraps OpenAIResponsesCompactionSession.add_items.
// Add new items to the conversation history.
func (w *OpenAIResponsesCompactionSession) AddItems(items any) error {
	fn := w.obj.Attr("add_items")
	if fn == nil {
		return fmt.Errorf("add_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(items)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ClearSession wraps OpenAIResponsesCompactionSession.clear_session.
// Clear all items for this session.
func (w *OpenAIResponsesCompactionSession) ClearSession() error {
	fn := w.obj.Attr("clear_session")
	if fn == nil {
		return fmt.Errorf("clear_session not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetItems wraps OpenAIResponsesCompactionSession.get_items.
// Retrieve the conversation history for this session.
func (w *OpenAIResponsesCompactionSession) GetItems(limit any) (any, error) {
	fn := w.obj.Attr("get_items")
	if fn == nil {
		return nil, fmt.Errorf("get_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(limit)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PopItem wraps OpenAIResponsesCompactionSession.pop_item.
// Remove and return the most recent item from the session.
func (w *OpenAIResponsesCompactionSession) PopItem() (any, error) {
	fn := w.obj.Attr("pop_item")
	if fn == nil {
		return nil, fmt.Errorf("pop_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunCompaction wraps OpenAIResponsesCompactionSession.run_compaction.
// Run compaction using responses.compact API.
func (w *OpenAIResponsesCompactionSession) RunCompaction(args *OpenAIResponsesCompactionArgs) error {
	fn := w.obj.Attr("run_compaction")
	if fn == nil {
		return fmt.Errorf("run_compaction not found")
	}
	defer fn.Close()
	result, err := fn.Call(args)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// OpenAIResponsesModel wraps a Python OpenAIResponsesModel instance.
// Implementation of `Model` that uses the OpenAI Responses API.
type OpenAIResponsesModel struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIResponsesModel creates a new OpenAIResponsesModel instance.
func (m *Module) NewOpenAIResponsesModel(model any, openaiClient *AsyncOpenAI, modelIsExplicit bool) (*OpenAIResponsesModel, error) {
	cls := m.mod.Attr("OpenAIResponsesModel")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIResponsesModel not found")
	}
	defer cls.Close()
	obj, err := cls.Call(model, openaiClient, modelIsExplicit)
	if err != nil {
		return nil, err
	}
	return &OpenAIResponsesModel{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIResponsesModel) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIResponsesModel) Object() *pyffi.Object {
	return w.obj
}

// GetResponse wraps OpenAIResponsesModel.get_response.
// Get a response from the model.
func (w *OpenAIResponsesModel) GetResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (*ModelResponse, error) {
	fn := w.obj.Attr("get_response")
	if fn == nil {
		return nil, fmt.Errorf("get_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	return &ModelResponse{obj: result, rt: w.rt}, nil
}

// GetRetryAdvice wraps OpenAIResponsesModel.get_retry_advice.
// Return provider-specific retry guidance for a failed model request.
func (w *OpenAIResponsesModel) GetRetryAdvice(request *ModelRetryAdviceRequest) (*ModelRetryAdvice, error) {
	fn := w.obj.Attr("get_retry_advice")
	if fn == nil {
		return nil, fmt.Errorf("get_retry_advice not found")
	}
	defer fn.Close()
	result, err := fn.Call(request)
	if err != nil {
		return nil, err
	}
	return &ModelRetryAdvice{obj: result, rt: w.rt}, nil
}

// StreamResponse wraps OpenAIResponsesModel.stream_response.
// Yields a partial message as it is generated, as well as the usage information.
func (w *OpenAIResponsesModel) StreamResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (any, error) {
	fn := w.obj.Attr("stream_response")
	if fn == nil {
		return nil, fmt.Errorf("stream_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// OpenAIResponsesWSModel wraps a Python OpenAIResponsesWSModel instance.
// Implementation of `Model` that uses the OpenAI Responses API over a websocket transport.
type OpenAIResponsesWSModel struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOpenAIResponsesWSModel creates a new OpenAIResponsesWSModel instance.
func (m *Module) NewOpenAIResponsesWSModel(model any, openaiClient *AsyncOpenAI, modelIsExplicit bool) (*OpenAIResponsesWSModel, error) {
	cls := m.mod.Attr("OpenAIResponsesWSModel")
	if cls == nil {
		return nil, fmt.Errorf("class OpenAIResponsesWSModel not found")
	}
	defer cls.Close()
	obj, err := cls.Call(model, openaiClient, modelIsExplicit)
	if err != nil {
		return nil, err
	}
	return &OpenAIResponsesWSModel{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OpenAIResponsesWSModel) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OpenAIResponsesWSModel) Object() *pyffi.Object {
	return w.obj
}

// GetResponse wraps OpenAIResponsesWSModel.get_response.
// Get a response from the model.
func (w *OpenAIResponsesWSModel) GetResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (*ModelResponse, error) {
	fn := w.obj.Attr("get_response")
	if fn == nil {
		return nil, fmt.Errorf("get_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	return &ModelResponse{obj: result, rt: w.rt}, nil
}

// GetRetryAdvice wraps OpenAIResponsesWSModel.get_retry_advice.
// Return provider-specific retry guidance for a failed model request.
func (w *OpenAIResponsesWSModel) GetRetryAdvice(request *ModelRetryAdviceRequest) (*ModelRetryAdvice, error) {
	fn := w.obj.Attr("get_retry_advice")
	if fn == nil {
		return nil, fmt.Errorf("get_retry_advice not found")
	}
	defer fn.Close()
	result, err := fn.Call(request)
	if err != nil {
		return nil, err
	}
	return &ModelRetryAdvice{obj: result, rt: w.rt}, nil
}

// StreamResponse wraps OpenAIResponsesWSModel.stream_response.
// Yields a partial message as it is generated, as well as the usage information.
func (w *OpenAIResponsesWSModel) StreamResponse(systemInstructions any, input any, modelSettings *ModelSettings, tools any, outputSchema *AgentOutputSchemaBase, handoffs any, tracing any, previousResponseId any, conversationId any, prompt any) (any, error) {
	fn := w.obj.Attr("stream_response")
	if fn == nil {
		return nil, fmt.Errorf("stream_response not found")
	}
	defer fn.Close()
	result, err := fn.Call(systemInstructions, input, modelSettings, tools, outputSchema, handoffs, tracing, previousResponseId, conversationId, prompt)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// OutputGuardrail wraps a Python OutputGuardrail instance.
// Output guardrails are checks that run on the final output of an agent.
type OutputGuardrail struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOutputGuardrail creates a new OutputGuardrail instance.
func (m *Module) NewOutputGuardrail(guardrailFunction any, name any) (*OutputGuardrail, error) {
	cls := m.mod.Attr("OutputGuardrail")
	if cls == nil {
		return nil, fmt.Errorf("class OutputGuardrail not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrailFunction, name)
	if err != nil {
		return nil, err
	}
	return &OutputGuardrail{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OutputGuardrail) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OutputGuardrail) Object() *pyffi.Object {
	return w.obj
}

// GetName wraps OutputGuardrail.get_name.
func (w *OutputGuardrail) GetName() (string, error) {
	fn := w.obj.Attr("get_name")
	if fn == nil {
		return "", fmt.Errorf("get_name not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Run wraps OutputGuardrail.run.
func (w *OutputGuardrail) Run(context any, agent any, agentOutput any) (*OutputGuardrailResult, error) {
	fn := w.obj.Attr("run")
	if fn == nil {
		return nil, fmt.Errorf("run not found")
	}
	defer fn.Close()
	result, err := fn.Call(context, agent, agentOutput)
	if err != nil {
		return nil, err
	}
	return &OutputGuardrailResult{obj: result, rt: w.rt}, nil
}

// OutputGuardrailResult wraps a Python OutputGuardrailResult instance.
// The result of a guardrail run.
type OutputGuardrailResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOutputGuardrailResult creates a new OutputGuardrailResult instance.
func (m *Module) NewOutputGuardrailResult(guardrail any, agentOutput any, agent any, output *GuardrailFunctionOutput) (*OutputGuardrailResult, error) {
	cls := m.mod.Attr("OutputGuardrailResult")
	if cls == nil {
		return nil, fmt.Errorf("class OutputGuardrailResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrail, agentOutput, agent, output)
	if err != nil {
		return nil, err
	}
	return &OutputGuardrailResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OutputGuardrailResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OutputGuardrailResult) Object() *pyffi.Object {
	return w.obj
}

// OutputGuardrailTripwireTriggered wraps a Python OutputGuardrailTripwireTriggered instance.
// Exception raised when a guardrail tripwire is triggered.
type OutputGuardrailTripwireTriggered struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewOutputGuardrailTripwireTriggered creates a new OutputGuardrailTripwireTriggered instance.
func (m *Module) NewOutputGuardrailTripwireTriggered(guardrailResult *OutputGuardrailResult) (*OutputGuardrailTripwireTriggered, error) {
	cls := m.mod.Attr("OutputGuardrailTripwireTriggered")
	if cls == nil {
		return nil, fmt.Errorf("class OutputGuardrailTripwireTriggered not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrailResult)
	if err != nil {
		return nil, err
	}
	return &OutputGuardrailTripwireTriggered{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *OutputGuardrailTripwireTriggered) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *OutputGuardrailTripwireTriggered) Object() *pyffi.Object {
	return w.obj
}

// Prompt wraps a Python Prompt instance.
// Prompt configuration to use for interacting with an OpenAI model.
type Prompt struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewPrompt creates a new Prompt instance.
func (m *Module) NewPrompt() (*Prompt, error) {
	cls := m.mod.Attr("Prompt")
	if cls == nil {
		return nil, fmt.Errorf("class Prompt not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Prompt{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Prompt) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Prompt) Object() *pyffi.Object {
	return w.obj
}

// RawResponsesStreamEvent wraps a Python RawResponsesStreamEvent instance.
// Streaming event from the LLM. These are 'raw' events, i.e. they are directly passed through
type RawResponsesStreamEvent struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRawResponsesStreamEvent creates a new RawResponsesStreamEvent instance.
func (m *Module) NewRawResponsesStreamEvent(data any, type_ any) (*RawResponsesStreamEvent, error) {
	cls := m.mod.Attr("RawResponsesStreamEvent")
	if cls == nil {
		return nil, fmt.Errorf("class RawResponsesStreamEvent not found")
	}
	defer cls.Close()
	obj, err := cls.Call(data, type_)
	if err != nil {
		return nil, err
	}
	return &RawResponsesStreamEvent{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RawResponsesStreamEvent) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RawResponsesStreamEvent) Object() *pyffi.Object {
	return w.obj
}

// ReasoningItem wraps a Python ReasoningItem instance.
// Represents a reasoning item.
type ReasoningItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewReasoningItem creates a new ReasoningItem instance.
func (m *Module) NewReasoningItem(agent any, rawItem any, type_ any) (*ReasoningItem, error) {
	cls := m.mod.Attr("ReasoningItem")
	if cls == nil {
		return nil, fmt.Errorf("class ReasoningItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_)
	if err != nil {
		return nil, err
	}
	return &ReasoningItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ReasoningItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ReasoningItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps ReasoningItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *ReasoningItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps ReasoningItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *ReasoningItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ResponsesWebSocketSession wraps a Python ResponsesWebSocketSession instance.
// Helper that pins runs to a shared OpenAI websocket-capable provider.
type ResponsesWebSocketSession struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewResponsesWebSocketSession creates a new ResponsesWebSocketSession instance.
func (m *Module) NewResponsesWebSocketSession(provider *OpenAIProvider, runConfig *RunConfig) (*ResponsesWebSocketSession, error) {
	cls := m.mod.Attr("ResponsesWebSocketSession")
	if cls == nil {
		return nil, fmt.Errorf("class ResponsesWebSocketSession not found")
	}
	defer cls.Close()
	obj, err := cls.Call(provider, runConfig)
	if err != nil {
		return nil, err
	}
	return &ResponsesWebSocketSession{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ResponsesWebSocketSession) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ResponsesWebSocketSession) Object() *pyffi.Object {
	return w.obj
}

// Aclose wraps ResponsesWebSocketSession.aclose.
// Close cached provider model resources (including websocket connections).
func (w *ResponsesWebSocketSession) Aclose() error {
	fn := w.obj.Attr("aclose")
	if fn == nil {
		return fmt.Errorf("aclose not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Run wraps ResponsesWebSocketSession.run.
// Call “Runner.run“ with the session's shared “RunConfig“.
func (w *ResponsesWebSocketSession) Run(startingAgent any, input any) (*RunResult, error) {
	fn := w.obj.Attr("run")
	if fn == nil {
		return nil, fmt.Errorf("run not found")
	}
	defer fn.Close()
	result, err := fn.Call(startingAgent, input)
	if err != nil {
		return nil, err
	}
	return &RunResult{obj: result, rt: w.rt}, nil
}

// RunStreamed wraps ResponsesWebSocketSession.run_streamed.
// Call “Runner.run_streamed“ with the session's shared “RunConfig“.
func (w *ResponsesWebSocketSession) RunStreamed(startingAgent any, input any) (*RunResultStreaming, error) {
	fn := w.obj.Attr("run_streamed")
	if fn == nil {
		return nil, fmt.Errorf("run_streamed not found")
	}
	defer fn.Close()
	result, err := fn.Call(startingAgent, input)
	if err != nil {
		return nil, err
	}
	return &RunResultStreaming{obj: result, rt: w.rt}, nil
}

// RetryDecision wraps a Python RetryDecision instance.
// Explicit retry decision returned by retry policies.
type RetryDecision struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRetryDecision creates a new RetryDecision instance.
func (m *Module) NewRetryDecision(retry bool, delay any, reason any) (*RetryDecision, error) {
	cls := m.mod.Attr("RetryDecision")
	if cls == nil {
		return nil, fmt.Errorf("class RetryDecision not found")
	}
	defer cls.Close()
	obj, err := cls.Call(retry, delay, reason)
	if err != nil {
		return nil, err
	}
	return &RetryDecision{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RetryDecision) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RetryDecision) Object() *pyffi.Object {
	return w.obj
}

// RetryPolicyContext wraps a Python RetryPolicyContext instance.
// Context passed to runtime retry policy callbacks.
type RetryPolicyContext struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRetryPolicyContext creates a new RetryPolicyContext instance.
func (m *Module) NewRetryPolicyContext(error any, attempt int64, maxRetries int64, stream bool, normalized *ModelRetryNormalizedError, providerAdvice *ModelRetryAdvice) (*RetryPolicyContext, error) {
	cls := m.mod.Attr("RetryPolicyContext")
	if cls == nil {
		return nil, fmt.Errorf("class RetryPolicyContext not found")
	}
	defer cls.Close()
	obj, err := cls.Call(error, attempt, maxRetries, stream, normalized, providerAdvice)
	if err != nil {
		return nil, err
	}
	return &RetryPolicyContext{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RetryPolicyContext) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RetryPolicyContext) Object() *pyffi.Object {
	return w.obj
}

// RunConfig wraps a Python RunConfig instance.
// Configures settings for the entire agent run.
type RunConfig struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunConfig creates a new RunConfig instance.
func (m *Module) NewRunConfig(model any, modelProvider *ModelProvider, modelSettings *ModelSettings, handoffInputFilter any, nestHandoffHistory bool, handoffHistoryMapper any, inputGuardrails any, outputGuardrails any, tracingDisabled bool, tracing any, traceIncludeSensitiveData bool, workflowName string, traceId any, groupId any, traceMetadata any, sessionInputCallback any, callModelInputFilter any, toolErrorFormatter any, sessionSettings *SessionSettings, reasoningItemIdPolicy any) (*RunConfig, error) {
	cls := m.mod.Attr("RunConfig")
	if cls == nil {
		return nil, fmt.Errorf("class RunConfig not found")
	}
	defer cls.Close()
	obj, err := cls.Call(model, modelProvider, modelSettings, handoffInputFilter, nestHandoffHistory, handoffHistoryMapper, inputGuardrails, outputGuardrails, tracingDisabled, tracing, traceIncludeSensitiveData, workflowName, traceId, groupId, traceMetadata, sessionInputCallback, callModelInputFilter, toolErrorFormatter, sessionSettings, reasoningItemIdPolicy)
	if err != nil {
		return nil, err
	}
	return &RunConfig{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunConfig) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunConfig) Object() *pyffi.Object {
	return w.obj
}

// RunContextWrapper wraps a Python RunContextWrapper instance.
// This wraps the context object that you passed to `Runner.run()`. It also contains
type RunContextWrapper struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunContextWrapper creates a new RunContextWrapper instance.
func (m *Module) NewRunContextWrapper(context any, usage *Usage, turnInput any, Approvals any, toolInput any) (*RunContextWrapper, error) {
	cls := m.mod.Attr("RunContextWrapper")
	if cls == nil {
		return nil, fmt.Errorf("class RunContextWrapper not found")
	}
	defer cls.Close()
	obj, err := cls.Call(context, usage, turnInput, Approvals, toolInput)
	if err != nil {
		return nil, err
	}
	return &RunContextWrapper{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunContextWrapper) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunContextWrapper) Object() *pyffi.Object {
	return w.obj
}

// ApproveTool wraps RunContextWrapper.approve_tool.
// Approve a tool call, optionally for all future calls.
func (w *RunContextWrapper) ApproveTool(approvalItem any, alwaysApprove bool) error {
	fn := w.obj.Attr("approve_tool")
	if fn == nil {
		return fmt.Errorf("approve_tool not found")
	}
	defer fn.Close()
	result, err := fn.Call(approvalItem, alwaysApprove)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetApprovalStatus wraps RunContextWrapper.get_approval_status.
// Return approval status, retrying with pending item's tool name if necessary.
func (w *RunContextWrapper) GetApprovalStatus(toolName string, callId string, toolNamespace any, existingPending any, toolLookupKey any) (any, error) {
	fn := w.obj.Attr("get_approval_status")
	if fn == nil {
		return nil, fmt.Errorf("get_approval_status not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, callId, toolNamespace, existingPending, toolLookupKey)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetRejectionMessage wraps RunContextWrapper.get_rejection_message.
// Return a stored rejection message for a tool call if one exists.
func (w *RunContextWrapper) GetRejectionMessage(toolName string, callId string, toolNamespace any, existingPending any, toolLookupKey any) (any, error) {
	fn := w.obj.Attr("get_rejection_message")
	if fn == nil {
		return nil, fmt.Errorf("get_rejection_message not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, callId, toolNamespace, existingPending, toolLookupKey)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// IsToolApproved wraps RunContextWrapper.is_tool_approved.
// Return True/False/None for the given tool call.
func (w *RunContextWrapper) IsToolApproved(toolName string, callId string) (any, error) {
	fn := w.obj.Attr("is_tool_approved")
	if fn == nil {
		return nil, fmt.Errorf("is_tool_approved not found")
	}
	defer fn.Close()
	result, err := fn.Call(toolName, callId)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RejectTool wraps RunContextWrapper.reject_tool.
// Reject a tool call, optionally for all future calls.
func (w *RunContextWrapper) RejectTool(approvalItem any, alwaysReject bool, rejectionMessage any) error {
	fn := w.obj.Attr("reject_tool")
	if fn == nil {
		return fmt.Errorf("reject_tool not found")
	}
	defer fn.Close()
	result, err := fn.Call(approvalItem, alwaysReject, rejectionMessage)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// RunErrorData wraps a Python RunErrorData instance.
// Snapshot of run data passed to error handlers.
type RunErrorData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunErrorData creates a new RunErrorData instance.
func (m *Module) NewRunErrorData(input any, newItems any, history any, output any, rawResponses any, lastAgent any) (*RunErrorData, error) {
	cls := m.mod.Attr("RunErrorData")
	if cls == nil {
		return nil, fmt.Errorf("class RunErrorData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, newItems, history, output, rawResponses, lastAgent)
	if err != nil {
		return nil, err
	}
	return &RunErrorData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunErrorData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunErrorData) Object() *pyffi.Object {
	return w.obj
}

// RunErrorDetails wraps a Python RunErrorDetails instance.
// Data collected from an agent run when an exception occurs.
type RunErrorDetails struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunErrorDetails creates a new RunErrorDetails instance.
func (m *Module) NewRunErrorDetails(input any, newItems any, rawResponses any, lastAgent any, contextWrapper any, inputGuardrailResults any, outputGuardrailResults any) (*RunErrorDetails, error) {
	cls := m.mod.Attr("RunErrorDetails")
	if cls == nil {
		return nil, fmt.Errorf("class RunErrorDetails not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, newItems, rawResponses, lastAgent, contextWrapper, inputGuardrailResults, outputGuardrailResults)
	if err != nil {
		return nil, err
	}
	return &RunErrorDetails{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunErrorDetails) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunErrorDetails) Object() *pyffi.Object {
	return w.obj
}

// RunErrorHandlerInput wraps a Python RunErrorHandlerInput instance.
// RunErrorHandlerInput(error: 'MaxTurnsExceeded', context: 'RunContextWrapper[TContext]', run_data: 'RunErrorData')
type RunErrorHandlerInput struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunErrorHandlerInput creates a new RunErrorHandlerInput instance.
func (m *Module) NewRunErrorHandlerInput(error *MaxTurnsExceeded, context any, runData *RunErrorData) (*RunErrorHandlerInput, error) {
	cls := m.mod.Attr("RunErrorHandlerInput")
	if cls == nil {
		return nil, fmt.Errorf("class RunErrorHandlerInput not found")
	}
	defer cls.Close()
	obj, err := cls.Call(error, context, runData)
	if err != nil {
		return nil, err
	}
	return &RunErrorHandlerInput{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunErrorHandlerInput) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunErrorHandlerInput) Object() *pyffi.Object {
	return w.obj
}

// RunErrorHandlerResult wraps a Python RunErrorHandlerResult instance.
// Result returned by an error handler.
type RunErrorHandlerResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunErrorHandlerResult creates a new RunErrorHandlerResult instance.
func (m *Module) NewRunErrorHandlerResult(finalOutput any, includeInHistory bool) (*RunErrorHandlerResult, error) {
	cls := m.mod.Attr("RunErrorHandlerResult")
	if cls == nil {
		return nil, fmt.Errorf("class RunErrorHandlerResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(finalOutput, includeInHistory)
	if err != nil {
		return nil, err
	}
	return &RunErrorHandlerResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunErrorHandlerResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunErrorHandlerResult) Object() *pyffi.Object {
	return w.obj
}

// RunErrorHandlers wraps a Python RunErrorHandlers instance.
// Error handlers keyed by error kind.
type RunErrorHandlers struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunErrorHandlers creates a new RunErrorHandlers instance.
func (m *Module) NewRunErrorHandlers() (*RunErrorHandlers, error) {
	cls := m.mod.Attr("RunErrorHandlers")
	if cls == nil {
		return nil, fmt.Errorf("class RunErrorHandlers not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &RunErrorHandlers{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunErrorHandlers) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunErrorHandlers) Object() *pyffi.Object {
	return w.obj
}

// RunItemStreamEvent wraps a Python RunItemStreamEvent instance.
// Streaming events that wrap a `RunItem`. As the agent processes the LLM response, it will
type RunItemStreamEvent struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunItemStreamEvent creates a new RunItemStreamEvent instance.
func (m *Module) NewRunItemStreamEvent(name any, item any, type_ any) (*RunItemStreamEvent, error) {
	cls := m.mod.Attr("RunItemStreamEvent")
	if cls == nil {
		return nil, fmt.Errorf("class RunItemStreamEvent not found")
	}
	defer cls.Close()
	obj, err := cls.Call(name, item, type_)
	if err != nil {
		return nil, err
	}
	return &RunItemStreamEvent{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunItemStreamEvent) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunItemStreamEvent) Object() *pyffi.Object {
	return w.obj
}

// RunResult wraps a Python RunResult instance.
// RunResult(input: 'str | list[TResponseInputItem]', new_items: 'list[RunItem]', raw_responses: 'list[ModelResponse]', final_output: 'Any', input_guardrail_results: 'list[InputGuardrailResult]', output_guardrail_results: 'list[OutputGuardrailResult]', tool_input_guardrail_results: 'list[ToolInputGuardrailResult]', tool_output_guardrail_results: 'list[ToolOutputGuardrailResult]', context_wrapper: 'RunContextWrapper[Any]', _last_agent: 'Agent[Any]', _last_processed_response: 'ProcessedResponse | None' = None, _tool_use_tracker_snapshot: 'dict[str, list[str]]' = <factory>, _current_turn_persisted_item_count: 'int' = 0, _current_turn: 'int' = 0, _model_input_items: 'list[RunItem]' = <factory>, _original_input: 'str | list[TResponseInputItem] | None' = None, _conversation_id: 'str | None' = None, _previous_response_id: 'str | None' = None, _auto_previous_response_id: 'bool' = False, max_turns: 'int' = 10, interruptions: 'list[ToolApprovalItem]' = <factory>)
type RunResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunResult creates a new RunResult instance.
func (m *Module) NewRunResult(input any, newItems any, rawResponses any, finalOutput any, inputGuardrailResults any, outputGuardrailResults any, toolInputGuardrailResults any, toolOutputGuardrailResults any, contextWrapper any, LastAgent any, LastProcessedResponse any, ToolUseTrackerSnapshot any, CurrentTurnPersistedItemCount int64, CurrentTurn int64, ModelInputItems any, OriginalInput any, ConversationId any, PreviousResponseId any, AutoPreviousResponseId bool, maxTurns int64, interruptions any) (*RunResult, error) {
	cls := m.mod.Attr("RunResult")
	if cls == nil {
		return nil, fmt.Errorf("class RunResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, newItems, rawResponses, finalOutput, inputGuardrailResults, outputGuardrailResults, toolInputGuardrailResults, toolOutputGuardrailResults, contextWrapper, LastAgent, LastProcessedResponse, ToolUseTrackerSnapshot, CurrentTurnPersistedItemCount, CurrentTurn, ModelInputItems, OriginalInput, ConversationId, PreviousResponseId, AutoPreviousResponseId, maxTurns, interruptions)
	if err != nil {
		return nil, err
	}
	return &RunResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunResult) Object() *pyffi.Object {
	return w.obj
}

// FinalOutputAs wraps RunResult.final_output_as.
// A convenience method to cast the final output to a specific type. By default, the cast
func (w *RunResult) FinalOutputAs(cls any, raiseIfIncorrectType bool) (any, error) {
	fn := w.obj.Attr("final_output_as")
	if fn == nil {
		return nil, fmt.Errorf("final_output_as not found")
	}
	defer fn.Close()
	result, err := fn.Call(cls, raiseIfIncorrectType)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ReleaseAgents wraps RunResult.release_agents.
// Release strong references to agents held by this result. After calling this method,
func (w *RunResult) ReleaseAgents(releaseNewItems bool) error {
	fn := w.obj.Attr("release_agents")
	if fn == nil {
		return fmt.Errorf("release_agents not found")
	}
	defer fn.Close()
	result, err := fn.Call(releaseNewItems)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputList wraps RunResult.to_input_list.
// Create an input-item view of this run.
func (w *RunResult) ToInputList(mode any) (any, error) {
	fn := w.obj.Attr("to_input_list")
	if fn == nil {
		return nil, fmt.Errorf("to_input_list not found")
	}
	defer fn.Close()
	result, err := fn.Call(mode)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToState wraps RunResult.to_state.
// Create a RunState from this result to resume execution.
func (w *RunResult) ToState() (any, error) {
	fn := w.obj.Attr("to_state")
	if fn == nil {
		return nil, fmt.Errorf("to_state not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunResultStreaming wraps a Python RunResultStreaming instance.
// The result of an agent run in streaming mode. You can use the `stream_events` method to
type RunResultStreaming struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunResultStreaming creates a new RunResultStreaming instance.
func (m *Module) NewRunResultStreaming(input any, newItems any, rawResponses any, finalOutput any, inputGuardrailResults any, outputGuardrailResults any, toolInputGuardrailResults any, toolOutputGuardrailResults any, contextWrapper any, currentAgent any, currentTurn int64, maxTurns int64, CurrentAgentOutputSchema *AgentOutputSchemaBase, trace *Trace, isComplete bool, ModelInputItems any, EventQueue any, InputGuardrailQueue any, runLoopTask any, InputGuardrailsTask any, OutputGuardrailsTask any, StoredException any, CancelMode any, LastProcessedResponse any, interruptions any, WaitingOnEventQueue bool, CurrentTurnPersistedItemCount int64, StreamInputPersisted bool, OriginalInputForPersistence any, MaxTurnsHandled bool, OriginalInput any, ToolUseTrackerSnapshot any, State any, ConversationId any, PreviousResponseId any, AutoPreviousResponseId bool, RunImplTask any) (*RunResultStreaming, error) {
	cls := m.mod.Attr("RunResultStreaming")
	if cls == nil {
		return nil, fmt.Errorf("class RunResultStreaming not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, newItems, rawResponses, finalOutput, inputGuardrailResults, outputGuardrailResults, toolInputGuardrailResults, toolOutputGuardrailResults, contextWrapper, currentAgent, currentTurn, maxTurns, CurrentAgentOutputSchema, trace, isComplete, ModelInputItems, EventQueue, InputGuardrailQueue, runLoopTask, InputGuardrailsTask, OutputGuardrailsTask, StoredException, CancelMode, LastProcessedResponse, interruptions, WaitingOnEventQueue, CurrentTurnPersistedItemCount, StreamInputPersisted, OriginalInputForPersistence, MaxTurnsHandled, OriginalInput, ToolUseTrackerSnapshot, State, ConversationId, PreviousResponseId, AutoPreviousResponseId, RunImplTask)
	if err != nil {
		return nil, err
	}
	return &RunResultStreaming{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunResultStreaming) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunResultStreaming) Object() *pyffi.Object {
	return w.obj
}

// Cancel wraps RunResultStreaming.cancel.
// Cancel the streaming run.
func (w *RunResultStreaming) Cancel(mode any) error {
	fn := w.obj.Attr("cancel")
	if fn == nil {
		return fmt.Errorf("cancel not found")
	}
	defer fn.Close()
	result, err := fn.Call(mode)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// FinalOutputAs wraps RunResultStreaming.final_output_as.
// A convenience method to cast the final output to a specific type. By default, the cast
func (w *RunResultStreaming) FinalOutputAs(cls any, raiseIfIncorrectType bool) (any, error) {
	fn := w.obj.Attr("final_output_as")
	if fn == nil {
		return nil, fmt.Errorf("final_output_as not found")
	}
	defer fn.Close()
	result, err := fn.Call(cls, raiseIfIncorrectType)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ReleaseAgents wraps RunResultStreaming.release_agents.
// Release strong references to agents held by this result. After calling this method,
func (w *RunResultStreaming) ReleaseAgents(releaseNewItems bool) error {
	fn := w.obj.Attr("release_agents")
	if fn == nil {
		return fmt.Errorf("release_agents not found")
	}
	defer fn.Close()
	result, err := fn.Call(releaseNewItems)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// StreamEvents wraps RunResultStreaming.stream_events.
// Stream deltas for new items as they are generated. We're using the types from the
func (w *RunResultStreaming) StreamEvents() (any, error) {
	fn := w.obj.Attr("stream_events")
	if fn == nil {
		return nil, fmt.Errorf("stream_events not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToInputList wraps RunResultStreaming.to_input_list.
// Create an input-item view of this run.
func (w *RunResultStreaming) ToInputList(mode any) (any, error) {
	fn := w.obj.Attr("to_input_list")
	if fn == nil {
		return nil, fmt.Errorf("to_input_list not found")
	}
	defer fn.Close()
	result, err := fn.Call(mode)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToState wraps RunResultStreaming.to_state.
// Create a RunState from this streaming result to resume execution.
func (w *RunResultStreaming) ToState() (any, error) {
	fn := w.obj.Attr("to_state")
	if fn == nil {
		return nil, fmt.Errorf("to_state not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// RunState wraps a Python RunState instance.
// Serializable snapshot of an agent run, including context, usage, and interruptions.
type RunState struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunState creates a new RunState instance.
func (m *Module) NewRunState(context any, originalInput any, startingAgent any, maxTurns int64, conversationId any, previousResponseId any, autoPreviousResponseId bool) (*RunState, error) {
	cls := m.mod.Attr("RunState")
	if cls == nil {
		return nil, fmt.Errorf("class RunState not found")
	}
	defer cls.Close()
	obj, err := cls.Call(context, originalInput, startingAgent, maxTurns, conversationId, previousResponseId, autoPreviousResponseId)
	if err != nil {
		return nil, err
	}
	return &RunState{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *RunState) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *RunState) Object() *pyffi.Object {
	return w.obj
}

// Approve wraps RunState.approve.
// Approve a tool call and rerun with this state to continue.
func (w *RunState) Approve(approvalItem *ToolApprovalItem, alwaysApprove bool) error {
	fn := w.obj.Attr("approve")
	if fn == nil {
		return fmt.Errorf("approve not found")
	}
	defer fn.Close()
	result, err := fn.Call(approvalItem, alwaysApprove)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// FromJson wraps RunState.from_json.
// Deserializes a run state from a JSON dictionary.
func (w *RunState) FromJson(initialAgent any, stateJson any, contextOverride any, contextDeserializer any, strictContext bool) (any, error) {
	fn := w.obj.Attr("from_json")
	if fn == nil {
		return nil, fmt.Errorf("from_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(initialAgent, stateJson, contextOverride, contextDeserializer, strictContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// FromString wraps RunState.from_string.
// Deserializes a run state from a JSON string.
func (w *RunState) FromString(initialAgent any, stateString string, contextOverride any, contextDeserializer any, strictContext bool) (any, error) {
	fn := w.obj.Attr("from_string")
	if fn == nil {
		return nil, fmt.Errorf("from_string not found")
	}
	defer fn.Close()
	result, err := fn.Call(initialAgent, stateString, contextOverride, contextDeserializer, strictContext)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetInterruptions wraps RunState.get_interruptions.
// Return pending interruptions if the current step is an interruption.
func (w *RunState) GetInterruptions() (any, error) {
	fn := w.obj.Attr("get_interruptions")
	if fn == nil {
		return nil, fmt.Errorf("get_interruptions not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// GetToolUseTrackerSnapshot wraps RunState.get_tool_use_tracker_snapshot.
// Return a defensive copy of the tool-use tracker snapshot.
func (w *RunState) GetToolUseTrackerSnapshot() (any, error) {
	fn := w.obj.Attr("get_tool_use_tracker_snapshot")
	if fn == nil {
		return nil, fmt.Errorf("get_tool_use_tracker_snapshot not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Reject wraps RunState.reject.
// Reject a tool call and rerun with this state to continue.
func (w *RunState) Reject(approvalItem *ToolApprovalItem, alwaysReject bool, rejectionMessage any) error {
	fn := w.obj.Attr("reject")
	if fn == nil {
		return fmt.Errorf("reject not found")
	}
	defer fn.Close()
	result, err := fn.Call(approvalItem, alwaysReject, rejectionMessage)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetReasoningItemIdPolicy wraps RunState.set_reasoning_item_id_policy.
// Store how reasoning item IDs should appear in next-turn model input.
func (w *RunState) SetReasoningItemIdPolicy(policy any) error {
	fn := w.obj.Attr("set_reasoning_item_id_policy")
	if fn == nil {
		return fmt.Errorf("set_reasoning_item_id_policy not found")
	}
	defer fn.Close()
	result, err := fn.Call(policy)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetToolUseTrackerSnapshot wraps RunState.set_tool_use_tracker_snapshot.
// Store a copy of the serialized tool-use tracker data.
func (w *RunState) SetToolUseTrackerSnapshot(snapshot any) error {
	fn := w.obj.Attr("set_tool_use_tracker_snapshot")
	if fn == nil {
		return fmt.Errorf("set_tool_use_tracker_snapshot not found")
	}
	defer fn.Close()
	result, err := fn.Call(snapshot)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetTrace wraps RunState.set_trace.
// Capture trace metadata for serialization/resumption.
func (w *RunState) SetTrace(trace *Trace) error {
	fn := w.obj.Attr("set_trace")
	if fn == nil {
		return fmt.Errorf("set_trace not found")
	}
	defer fn.Close()
	result, err := fn.Call(trace)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToJson wraps RunState.to_json.
// Serializes the run state to a JSON-compatible dictionary.
func (w *RunState) ToJson(contextSerializer any, strictContext bool, includeTracingApiKey bool) (any, error) {
	fn := w.obj.Attr("to_json")
	if fn == nil {
		return nil, fmt.Errorf("to_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(contextSerializer, strictContext, includeTracingApiKey)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToString wraps RunState.to_string.
// Serializes the run state to a JSON string.
func (w *RunState) ToString(contextSerializer any, strictContext bool, includeTracingApiKey bool) (string, error) {
	fn := w.obj.Attr("to_string")
	if fn == nil {
		return "", fmt.Errorf("to_string not found")
	}
	defer fn.Close()
	result, err := fn.Call(contextSerializer, strictContext, includeTracingApiKey)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Runner wraps a Python Runner instance.
type Runner struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewRunner creates a new Runner instance.
func (m *Module) NewRunner() (*Runner, error) {
	cls := m.mod.Attr("Runner")
	if cls == nil {
		return nil, fmt.Errorf("class Runner not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Runner{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Runner) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Runner) Object() *pyffi.Object {
	return w.obj
}

// SQLiteSession wraps a Python SQLiteSession instance.
// SQLite-based implementation of session storage.
type SQLiteSession struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSQLiteSession creates a new SQLiteSession instance.
func (m *Module) NewSQLiteSession(sessionId string, dbPath any, sessionsTable string, messagesTable string, sessionSettings *SessionSettings) (*SQLiteSession, error) {
	cls := m.mod.Attr("SQLiteSession")
	if cls == nil {
		return nil, fmt.Errorf("class SQLiteSession not found")
	}
	defer cls.Close()
	obj, err := cls.Call(sessionId, dbPath, sessionsTable, messagesTable, sessionSettings)
	if err != nil {
		return nil, err
	}
	return &SQLiteSession{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SQLiteSession) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SQLiteSession) Object() *pyffi.Object {
	return w.obj
}

// AddItems wraps SQLiteSession.add_items.
// Add new items to the conversation history.
func (w *SQLiteSession) AddItems(items any) error {
	fn := w.obj.Attr("add_items")
	if fn == nil {
		return fmt.Errorf("add_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(items)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ClearSession wraps SQLiteSession.clear_session.
// Clear all items for this session.
func (w *SQLiteSession) ClearSession() error {
	fn := w.obj.Attr("clear_session")
	if fn == nil {
		return fmt.Errorf("clear_session not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetItems wraps SQLiteSession.get_items.
// Retrieve the conversation history for this session.
func (w *SQLiteSession) GetItems(limit any) (any, error) {
	fn := w.obj.Attr("get_items")
	if fn == nil {
		return nil, fmt.Errorf("get_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(limit)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PopItem wraps SQLiteSession.pop_item.
// Remove and return the most recent item from the session.
func (w *SQLiteSession) PopItem() (any, error) {
	fn := w.obj.Attr("pop_item")
	if fn == nil {
		return nil, fmt.Errorf("pop_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Session wraps a Python Session instance.
// Protocol for session implementations.
type Session struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSession creates a new Session instance.
func (m *Module) NewSession() (*Session, error) {
	cls := m.mod.Attr("Session")
	if cls == nil {
		return nil, fmt.Errorf("class Session not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Session{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Session) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Session) Object() *pyffi.Object {
	return w.obj
}

// AddItems wraps Session.add_items.
// Add new items to the conversation history.
func (w *Session) AddItems(items any) error {
	fn := w.obj.Attr("add_items")
	if fn == nil {
		return fmt.Errorf("add_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(items)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ClearSession wraps Session.clear_session.
// Clear all items for this session.
func (w *Session) ClearSession() error {
	fn := w.obj.Attr("clear_session")
	if fn == nil {
		return fmt.Errorf("clear_session not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetItems wraps Session.get_items.
// Retrieve the conversation history for this session.
func (w *Session) GetItems(limit any) (any, error) {
	fn := w.obj.Attr("get_items")
	if fn == nil {
		return nil, fmt.Errorf("get_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(limit)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PopItem wraps Session.pop_item.
// Remove and return the most recent item from the session.
func (w *Session) PopItem() (any, error) {
	fn := w.obj.Attr("pop_item")
	if fn == nil {
		return nil, fmt.Errorf("pop_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// SessionABC wraps a Python SessionABC instance.
// Abstract base class for session implementations.
type SessionABC struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSessionABC creates a new SessionABC instance.
func (m *Module) NewSessionABC() (*SessionABC, error) {
	cls := m.mod.Attr("SessionABC")
	if cls == nil {
		return nil, fmt.Errorf("class SessionABC not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &SessionABC{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SessionABC) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SessionABC) Object() *pyffi.Object {
	return w.obj
}

// AddItems wraps SessionABC.add_items.
// Add new items to the conversation history.
func (w *SessionABC) AddItems(items any) error {
	fn := w.obj.Attr("add_items")
	if fn == nil {
		return fmt.Errorf("add_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(items)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ClearSession wraps SessionABC.clear_session.
// Clear all items for this session.
func (w *SessionABC) ClearSession() error {
	fn := w.obj.Attr("clear_session")
	if fn == nil {
		return fmt.Errorf("clear_session not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// GetItems wraps SessionABC.get_items.
// Retrieve the conversation history for this session.
func (w *SessionABC) GetItems(limit any) (any, error) {
	fn := w.obj.Attr("get_items")
	if fn == nil {
		return nil, fmt.Errorf("get_items not found")
	}
	defer fn.Close()
	result, err := fn.Call(limit)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// PopItem wraps SessionABC.pop_item.
// Remove and return the most recent item from the session.
func (w *SessionABC) PopItem() (any, error) {
	fn := w.obj.Attr("pop_item")
	if fn == nil {
		return nil, fmt.Errorf("pop_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// SessionSettings wraps a Python SessionSettings instance.
// Settings for session operations.
type SessionSettings struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSessionSettings creates a new SessionSettings instance.
func (m *Module) NewSessionSettings(DataclassSelf any) (*SessionSettings, error) {
	cls := m.mod.Attr("SessionSettings")
	if cls == nil {
		return nil, fmt.Errorf("class SessionSettings not found")
	}
	defer cls.Close()
	obj, err := cls.Call(DataclassSelf)
	if err != nil {
		return nil, err
	}
	return &SessionSettings{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SessionSettings) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SessionSettings) Object() *pyffi.Object {
	return w.obj
}

// Resolve wraps SessionSettings.resolve.
// Produce a new SessionSettings by overlaying any non-None values from the
func (w *SessionSettings) Resolve(override *SessionSettings) (*SessionSettings, error) {
	fn := w.obj.Attr("resolve")
	if fn == nil {
		return nil, fmt.Errorf("resolve not found")
	}
	defer fn.Close()
	result, err := fn.Call(override)
	if err != nil {
		return nil, err
	}
	return &SessionSettings{obj: result, rt: w.rt}, nil
}

// ToDict wraps SessionSettings.to_dict.
// Convert settings to a dictionary.
func (w *SessionSettings) ToDict() (any, error) {
	fn := w.obj.Attr("to_dict")
	if fn == nil {
		return nil, fmt.Errorf("to_dict not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ShellActionRequest wraps a Python ShellActionRequest instance.
// Action payload for a next-generation shell call.
type ShellActionRequest struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellActionRequest creates a new ShellActionRequest instance.
func (m *Module) NewShellActionRequest(commands any, timeoutMs any, maxOutputLength any) (*ShellActionRequest, error) {
	cls := m.mod.Attr("ShellActionRequest")
	if cls == nil {
		return nil, fmt.Errorf("class ShellActionRequest not found")
	}
	defer cls.Close()
	obj, err := cls.Call(commands, timeoutMs, maxOutputLength)
	if err != nil {
		return nil, err
	}
	return &ShellActionRequest{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellActionRequest) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellActionRequest) Object() *pyffi.Object {
	return w.obj
}

// ShellCallData wraps a Python ShellCallData instance.
// Normalized shell call data provided to shell executors.
type ShellCallData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellCallData creates a new ShellCallData instance.
func (m *Module) NewShellCallData(callId string, action *ShellActionRequest, status any, raw any) (*ShellCallData, error) {
	cls := m.mod.Attr("ShellCallData")
	if cls == nil {
		return nil, fmt.Errorf("class ShellCallData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(callId, action, status, raw)
	if err != nil {
		return nil, err
	}
	return &ShellCallData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellCallData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellCallData) Object() *pyffi.Object {
	return w.obj
}

// ShellCallOutcome wraps a Python ShellCallOutcome instance.
// Describes the terminal condition of a shell command.
type ShellCallOutcome struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellCallOutcome creates a new ShellCallOutcome instance.
func (m *Module) NewShellCallOutcome(type_ any, exitCode any) (*ShellCallOutcome, error) {
	cls := m.mod.Attr("ShellCallOutcome")
	if cls == nil {
		return nil, fmt.Errorf("class ShellCallOutcome not found")
	}
	defer cls.Close()
	obj, err := cls.Call(type_, exitCode)
	if err != nil {
		return nil, err
	}
	return &ShellCallOutcome{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellCallOutcome) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellCallOutcome) Object() *pyffi.Object {
	return w.obj
}

// ShellCommandOutput wraps a Python ShellCommandOutput instance.
// Structured output for a single shell command execution.
type ShellCommandOutput struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellCommandOutput creates a new ShellCommandOutput instance.
func (m *Module) NewShellCommandOutput(stdout string, stderr string, outcome *ShellCallOutcome, command any, providerData any) (*ShellCommandOutput, error) {
	cls := m.mod.Attr("ShellCommandOutput")
	if cls == nil {
		return nil, fmt.Errorf("class ShellCommandOutput not found")
	}
	defer cls.Close()
	obj, err := cls.Call(stdout, stderr, outcome, command, providerData)
	if err != nil {
		return nil, err
	}
	return &ShellCommandOutput{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellCommandOutput) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellCommandOutput) Object() *pyffi.Object {
	return w.obj
}

// ShellCommandRequest wraps a Python ShellCommandRequest instance.
// A request to execute a modern shell call.
type ShellCommandRequest struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellCommandRequest creates a new ShellCommandRequest instance.
func (m *Module) NewShellCommandRequest(ctxWrapper any, data *ShellCallData) (*ShellCommandRequest, error) {
	cls := m.mod.Attr("ShellCommandRequest")
	if cls == nil {
		return nil, fmt.Errorf("class ShellCommandRequest not found")
	}
	defer cls.Close()
	obj, err := cls.Call(ctxWrapper, data)
	if err != nil {
		return nil, err
	}
	return &ShellCommandRequest{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellCommandRequest) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellCommandRequest) Object() *pyffi.Object {
	return w.obj
}

// ShellResult wraps a Python ShellResult instance.
// Result returned by a shell executor.
type ShellResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellResult creates a new ShellResult instance.
func (m *Module) NewShellResult(output any, maxOutputLength any, providerData any) (*ShellResult, error) {
	cls := m.mod.Attr("ShellResult")
	if cls == nil {
		return nil, fmt.Errorf("class ShellResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(output, maxOutputLength, providerData)
	if err != nil {
		return nil, err
	}
	return &ShellResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellResult) Object() *pyffi.Object {
	return w.obj
}

// ShellTool wraps a Python ShellTool instance.
// Next-generation shell tool. LocalShellTool will be deprecated in favor of this.
type ShellTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellTool creates a new ShellTool instance.
func (m *Module) NewShellTool(executor any, name string, needsApproval any, onApproval any, environment any) (*ShellTool, error) {
	cls := m.mod.Attr("ShellTool")
	if cls == nil {
		return nil, fmt.Errorf("class ShellTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(executor, name, needsApproval, onApproval, environment)
	if err != nil {
		return nil, err
	}
	return &ShellTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellTool) Object() *pyffi.Object {
	return w.obj
}

// ShellToolContainerAutoEnvironment wraps a Python ShellToolContainerAutoEnvironment instance.
// Auto-provisioned hosted container environment.
type ShellToolContainerAutoEnvironment struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolContainerAutoEnvironment creates a new ShellToolContainerAutoEnvironment instance.
func (m *Module) NewShellToolContainerAutoEnvironment() (*ShellToolContainerAutoEnvironment, error) {
	cls := m.mod.Attr("ShellToolContainerAutoEnvironment")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolContainerAutoEnvironment not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolContainerAutoEnvironment{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolContainerAutoEnvironment) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolContainerAutoEnvironment) Object() *pyffi.Object {
	return w.obj
}

// ShellToolContainerNetworkPolicyAllowlist wraps a Python ShellToolContainerNetworkPolicyAllowlist instance.
// Allowlist network policy for hosted containers.
type ShellToolContainerNetworkPolicyAllowlist struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolContainerNetworkPolicyAllowlist creates a new ShellToolContainerNetworkPolicyAllowlist instance.
func (m *Module) NewShellToolContainerNetworkPolicyAllowlist() (*ShellToolContainerNetworkPolicyAllowlist, error) {
	cls := m.mod.Attr("ShellToolContainerNetworkPolicyAllowlist")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolContainerNetworkPolicyAllowlist not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolContainerNetworkPolicyAllowlist{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolContainerNetworkPolicyAllowlist) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolContainerNetworkPolicyAllowlist) Object() *pyffi.Object {
	return w.obj
}

// ShellToolContainerNetworkPolicyDisabled wraps a Python ShellToolContainerNetworkPolicyDisabled instance.
// Disabled network policy for hosted containers.
type ShellToolContainerNetworkPolicyDisabled struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolContainerNetworkPolicyDisabled creates a new ShellToolContainerNetworkPolicyDisabled instance.
func (m *Module) NewShellToolContainerNetworkPolicyDisabled() (*ShellToolContainerNetworkPolicyDisabled, error) {
	cls := m.mod.Attr("ShellToolContainerNetworkPolicyDisabled")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolContainerNetworkPolicyDisabled not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolContainerNetworkPolicyDisabled{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolContainerNetworkPolicyDisabled) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolContainerNetworkPolicyDisabled) Object() *pyffi.Object {
	return w.obj
}

// ShellToolContainerNetworkPolicyDomainSecret wraps a Python ShellToolContainerNetworkPolicyDomainSecret instance.
// A secret bound to a single domain in allowlist mode.
type ShellToolContainerNetworkPolicyDomainSecret struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolContainerNetworkPolicyDomainSecret creates a new ShellToolContainerNetworkPolicyDomainSecret instance.
func (m *Module) NewShellToolContainerNetworkPolicyDomainSecret() (*ShellToolContainerNetworkPolicyDomainSecret, error) {
	cls := m.mod.Attr("ShellToolContainerNetworkPolicyDomainSecret")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolContainerNetworkPolicyDomainSecret not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolContainerNetworkPolicyDomainSecret{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolContainerNetworkPolicyDomainSecret) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolContainerNetworkPolicyDomainSecret) Object() *pyffi.Object {
	return w.obj
}

// ShellToolContainerReferenceEnvironment wraps a Python ShellToolContainerReferenceEnvironment instance.
// Reference to an existing hosted container.
type ShellToolContainerReferenceEnvironment struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolContainerReferenceEnvironment creates a new ShellToolContainerReferenceEnvironment instance.
func (m *Module) NewShellToolContainerReferenceEnvironment() (*ShellToolContainerReferenceEnvironment, error) {
	cls := m.mod.Attr("ShellToolContainerReferenceEnvironment")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolContainerReferenceEnvironment not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolContainerReferenceEnvironment{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolContainerReferenceEnvironment) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolContainerReferenceEnvironment) Object() *pyffi.Object {
	return w.obj
}

// ShellToolInlineSkill wraps a Python ShellToolInlineSkill instance.
// Inline hosted shell skill bundle.
type ShellToolInlineSkill struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolInlineSkill creates a new ShellToolInlineSkill instance.
func (m *Module) NewShellToolInlineSkill() (*ShellToolInlineSkill, error) {
	cls := m.mod.Attr("ShellToolInlineSkill")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolInlineSkill not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolInlineSkill{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolInlineSkill) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolInlineSkill) Object() *pyffi.Object {
	return w.obj
}

// ShellToolInlineSkillSource wraps a Python ShellToolInlineSkillSource instance.
// Inline skill source payload.
type ShellToolInlineSkillSource struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolInlineSkillSource creates a new ShellToolInlineSkillSource instance.
func (m *Module) NewShellToolInlineSkillSource() (*ShellToolInlineSkillSource, error) {
	cls := m.mod.Attr("ShellToolInlineSkillSource")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolInlineSkillSource not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolInlineSkillSource{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolInlineSkillSource) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolInlineSkillSource) Object() *pyffi.Object {
	return w.obj
}

// ShellToolLocalEnvironment wraps a Python ShellToolLocalEnvironment instance.
// Local shell execution environment.
type ShellToolLocalEnvironment struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolLocalEnvironment creates a new ShellToolLocalEnvironment instance.
func (m *Module) NewShellToolLocalEnvironment() (*ShellToolLocalEnvironment, error) {
	cls := m.mod.Attr("ShellToolLocalEnvironment")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolLocalEnvironment not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolLocalEnvironment{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolLocalEnvironment) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolLocalEnvironment) Object() *pyffi.Object {
	return w.obj
}

// ShellToolLocalSkill wraps a Python ShellToolLocalSkill instance.
// Skill metadata for local shell environments.
type ShellToolLocalSkill struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolLocalSkill creates a new ShellToolLocalSkill instance.
func (m *Module) NewShellToolLocalSkill() (*ShellToolLocalSkill, error) {
	cls := m.mod.Attr("ShellToolLocalSkill")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolLocalSkill not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolLocalSkill{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolLocalSkill) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolLocalSkill) Object() *pyffi.Object {
	return w.obj
}

// ShellToolSkillReference wraps a Python ShellToolSkillReference instance.
// Reference to a hosted shell skill.
type ShellToolSkillReference struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewShellToolSkillReference creates a new ShellToolSkillReference instance.
func (m *Module) NewShellToolSkillReference() (*ShellToolSkillReference, error) {
	cls := m.mod.Attr("ShellToolSkillReference")
	if cls == nil {
		return nil, fmt.Errorf("class ShellToolSkillReference not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ShellToolSkillReference{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ShellToolSkillReference) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ShellToolSkillReference) Object() *pyffi.Object {
	return w.obj
}

// Span wraps a Python Span instance.
// Base class for representing traceable operations with timing and context.
type Span struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSpan creates a new Span instance.
func (m *Module) NewSpan() (*Span, error) {
	cls := m.mod.Attr("Span")
	if cls == nil {
		return nil, fmt.Errorf("class Span not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Span{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Span) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Span) Object() *pyffi.Object {
	return w.obj
}

// Export wraps Span.export.
func (w *Span) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Finish wraps Span.finish.
// Finish the span.
func (w *Span) Finish(resetCurrent bool) error {
	fn := w.obj.Attr("finish")
	if fn == nil {
		return fmt.Errorf("finish not found")
	}
	defer fn.Close()
	result, err := fn.Call(resetCurrent)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// SetError wraps Span.set_error.
func (w *Span) SetError(error *SpanError) error {
	fn := w.obj.Attr("set_error")
	if fn == nil {
		return fmt.Errorf("set_error not found")
	}
	defer fn.Close()
	result, err := fn.Call(error)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Start wraps Span.start.
// Start the span.
func (w *Span) Start(markAsCurrent bool) (any, error) {
	fn := w.obj.Attr("start")
	if fn == nil {
		return nil, fmt.Errorf("start not found")
	}
	defer fn.Close()
	result, err := fn.Call(markAsCurrent)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// SpanData wraps a Python SpanData instance.
// Represents span data in the trace.
type SpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSpanData creates a new SpanData instance.
func (m *Module) NewSpanData() (*SpanData, error) {
	cls := m.mod.Attr("SpanData")
	if cls == nil {
		return nil, fmt.Errorf("class SpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &SpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps SpanData.export.
// Export the span data as a dictionary.
func (w *SpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// SpanError wraps a Python SpanError instance.
// Represents an error that occurred during span execution.
type SpanError struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSpanError creates a new SpanError instance.
func (m *Module) NewSpanError() (*SpanError, error) {
	cls := m.mod.Attr("SpanError")
	if cls == nil {
		return nil, fmt.Errorf("class SpanError not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &SpanError{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SpanError) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SpanError) Object() *pyffi.Object {
	return w.obj
}

// SpeechGroupSpanData wraps a Python SpeechGroupSpanData instance.
// Represents a Speech Group Span in the trace.
type SpeechGroupSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSpeechGroupSpanData creates a new SpeechGroupSpanData instance.
func (m *Module) NewSpeechGroupSpanData(input any) (*SpeechGroupSpanData, error) {
	cls := m.mod.Attr("SpeechGroupSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class SpeechGroupSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input)
	if err != nil {
		return nil, err
	}
	return &SpeechGroupSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SpeechGroupSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SpeechGroupSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps SpeechGroupSpanData.export.
// Export the span data as a dictionary.
func (w *SpeechGroupSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// SpeechSpanData wraps a Python SpeechSpanData instance.
// Represents a Speech Span in the trace.
type SpeechSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewSpeechSpanData creates a new SpeechSpanData instance.
func (m *Module) NewSpeechSpanData(input any, output any, outputFormat any, model any, modelConfig any, firstContentAt any) (*SpeechSpanData, error) {
	cls := m.mod.Attr("SpeechSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class SpeechSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, output, outputFormat, model, modelConfig, firstContentAt)
	if err != nil {
		return nil, err
	}
	return &SpeechSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *SpeechSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *SpeechSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps SpeechSpanData.export.
// Export the span data as a dictionary.
func (w *SpeechSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// StopAtTools wraps a Python StopAtTools instance.
// dict() -> new empty dictionary
type StopAtTools struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewStopAtTools creates a new StopAtTools instance.
func (m *Module) NewStopAtTools() (*StopAtTools, error) {
	cls := m.mod.Attr("StopAtTools")
	if cls == nil {
		return nil, fmt.Errorf("class StopAtTools not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &StopAtTools{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *StopAtTools) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *StopAtTools) Object() *pyffi.Object {
	return w.obj
}

// ToolApprovalItem wraps a Python ToolApprovalItem instance.
// Tool call that requires approval before execution.
type ToolApprovalItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolApprovalItem creates a new ToolApprovalItem instance.
func (m *Module) NewToolApprovalItem(agent any, rawItem any, toolName any, type_ any, toolNamespace any, AllowBareNameAlias bool, toolLookupKey any) (*ToolApprovalItem, error) {
	cls := m.mod.Attr("ToolApprovalItem")
	if cls == nil {
		return nil, fmt.Errorf("class ToolApprovalItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, toolName, type_, toolNamespace, AllowBareNameAlias, toolLookupKey)
	if err != nil {
		return nil, err
	}
	return &ToolApprovalItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolApprovalItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolApprovalItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps ToolApprovalItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *ToolApprovalItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps ToolApprovalItem.to_input_item.
// ToolApprovalItem should never be sent as input; raise to surface misuse.
func (w *ToolApprovalItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolCallItem wraps a Python ToolCallItem instance.
// Represents a tool call e.g. a function call or computer action call.
type ToolCallItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolCallItem creates a new ToolCallItem instance.
func (m *Module) NewToolCallItem(agent any, rawItem any, type_ any, description any, title any) (*ToolCallItem, error) {
	cls := m.mod.Attr("ToolCallItem")
	if cls == nil {
		return nil, fmt.Errorf("class ToolCallItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, type_, description, title)
	if err != nil {
		return nil, err
	}
	return &ToolCallItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolCallItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolCallItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps ToolCallItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *ToolCallItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps ToolCallItem.to_input_item.
// Converts this item into an input item suitable for passing to the model.
func (w *ToolCallItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolCallOutputItem wraps a Python ToolCallOutputItem instance.
// Represents the output of a tool call.
type ToolCallOutputItem struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolCallOutputItem creates a new ToolCallOutputItem instance.
func (m *Module) NewToolCallOutputItem(agent any, rawItem any, output any, type_ any) (*ToolCallOutputItem, error) {
	cls := m.mod.Attr("ToolCallOutputItem")
	if cls == nil {
		return nil, fmt.Errorf("class ToolCallOutputItem not found")
	}
	defer cls.Close()
	obj, err := cls.Call(agent, rawItem, output, type_)
	if err != nil {
		return nil, err
	}
	return &ToolCallOutputItem{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolCallOutputItem) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolCallOutputItem) Object() *pyffi.Object {
	return w.obj
}

// ReleaseAgent wraps ToolCallOutputItem.release_agent.
// Release the strong reference to the agent while keeping a weak reference.
func (w *ToolCallOutputItem) ReleaseAgent() error {
	fn := w.obj.Attr("release_agent")
	if fn == nil {
		return fmt.Errorf("release_agent not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToInputItem wraps ToolCallOutputItem.to_input_item.
// Converts the tool output into an input item for the next model turn.
func (w *ToolCallOutputItem) ToInputItem() (any, error) {
	fn := w.obj.Attr("to_input_item")
	if fn == nil {
		return nil, fmt.Errorf("to_input_item not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToolErrorFormatterArgs wraps a Python ToolErrorFormatterArgs instance.
// Data passed to “RunConfig.tool_error_formatter“ callbacks.
type ToolErrorFormatterArgs struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolErrorFormatterArgs creates a new ToolErrorFormatterArgs instance.
func (m *Module) NewToolErrorFormatterArgs(kind any, toolType any, toolName string, callId string, defaultMessage string, runContext any) (*ToolErrorFormatterArgs, error) {
	cls := m.mod.Attr("ToolErrorFormatterArgs")
	if cls == nil {
		return nil, fmt.Errorf("class ToolErrorFormatterArgs not found")
	}
	defer cls.Close()
	obj, err := cls.Call(kind, toolType, toolName, callId, defaultMessage, runContext)
	if err != nil {
		return nil, err
	}
	return &ToolErrorFormatterArgs{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolErrorFormatterArgs) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolErrorFormatterArgs) Object() *pyffi.Object {
	return w.obj
}

// ToolGuardrailFunctionOutput wraps a Python ToolGuardrailFunctionOutput instance.
// The output of a tool guardrail function.
type ToolGuardrailFunctionOutput struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolGuardrailFunctionOutput creates a new ToolGuardrailFunctionOutput instance.
func (m *Module) NewToolGuardrailFunctionOutput(outputInfo any, behavior any) (*ToolGuardrailFunctionOutput, error) {
	cls := m.mod.Attr("ToolGuardrailFunctionOutput")
	if cls == nil {
		return nil, fmt.Errorf("class ToolGuardrailFunctionOutput not found")
	}
	defer cls.Close()
	obj, err := cls.Call(outputInfo, behavior)
	if err != nil {
		return nil, err
	}
	return &ToolGuardrailFunctionOutput{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolGuardrailFunctionOutput) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolGuardrailFunctionOutput) Object() *pyffi.Object {
	return w.obj
}

// ToolInputGuardrail wraps a Python ToolInputGuardrail instance.
// A guardrail that runs before a function tool is invoked.
type ToolInputGuardrail struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolInputGuardrail creates a new ToolInputGuardrail instance.
func (m *Module) NewToolInputGuardrail(guardrailFunction any, name any) (*ToolInputGuardrail, error) {
	cls := m.mod.Attr("ToolInputGuardrail")
	if cls == nil {
		return nil, fmt.Errorf("class ToolInputGuardrail not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrailFunction, name)
	if err != nil {
		return nil, err
	}
	return &ToolInputGuardrail{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolInputGuardrail) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolInputGuardrail) Object() *pyffi.Object {
	return w.obj
}

// GetName wraps ToolInputGuardrail.get_name.
func (w *ToolInputGuardrail) GetName() (string, error) {
	fn := w.obj.Attr("get_name")
	if fn == nil {
		return "", fmt.Errorf("get_name not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Run wraps ToolInputGuardrail.run.
func (w *ToolInputGuardrail) Run(data *ToolInputGuardrailData) (*ToolGuardrailFunctionOutput, error) {
	fn := w.obj.Attr("run")
	if fn == nil {
		return nil, fmt.Errorf("run not found")
	}
	defer fn.Close()
	result, err := fn.Call(data)
	if err != nil {
		return nil, err
	}
	return &ToolGuardrailFunctionOutput{obj: result, rt: w.rt}, nil
}

// ToolInputGuardrailData wraps a Python ToolInputGuardrailData instance.
// Input data passed to a tool input guardrail function.
type ToolInputGuardrailData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolInputGuardrailData creates a new ToolInputGuardrailData instance.
func (m *Module) NewToolInputGuardrailData(context any, agent any) (*ToolInputGuardrailData, error) {
	cls := m.mod.Attr("ToolInputGuardrailData")
	if cls == nil {
		return nil, fmt.Errorf("class ToolInputGuardrailData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(context, agent)
	if err != nil {
		return nil, err
	}
	return &ToolInputGuardrailData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolInputGuardrailData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolInputGuardrailData) Object() *pyffi.Object {
	return w.obj
}

// ToolInputGuardrailResult wraps a Python ToolInputGuardrailResult instance.
// The result of a tool input guardrail run.
type ToolInputGuardrailResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolInputGuardrailResult creates a new ToolInputGuardrailResult instance.
func (m *Module) NewToolInputGuardrailResult(guardrail any, output *ToolGuardrailFunctionOutput) (*ToolInputGuardrailResult, error) {
	cls := m.mod.Attr("ToolInputGuardrailResult")
	if cls == nil {
		return nil, fmt.Errorf("class ToolInputGuardrailResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrail, output)
	if err != nil {
		return nil, err
	}
	return &ToolInputGuardrailResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolInputGuardrailResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolInputGuardrailResult) Object() *pyffi.Object {
	return w.obj
}

// ToolInputGuardrailTripwireTriggered wraps a Python ToolInputGuardrailTripwireTriggered instance.
// Exception raised when a tool input guardrail tripwire is triggered.
type ToolInputGuardrailTripwireTriggered struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolInputGuardrailTripwireTriggered creates a new ToolInputGuardrailTripwireTriggered instance.
func (m *Module) NewToolInputGuardrailTripwireTriggered(guardrail any, output *ToolGuardrailFunctionOutput) (*ToolInputGuardrailTripwireTriggered, error) {
	cls := m.mod.Attr("ToolInputGuardrailTripwireTriggered")
	if cls == nil {
		return nil, fmt.Errorf("class ToolInputGuardrailTripwireTriggered not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrail, output)
	if err != nil {
		return nil, err
	}
	return &ToolInputGuardrailTripwireTriggered{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolInputGuardrailTripwireTriggered) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolInputGuardrailTripwireTriggered) Object() *pyffi.Object {
	return w.obj
}

// ToolOutputFileContent wraps a Python ToolOutputFileContent instance.
// Represents a tool output that should be sent to the model as a file.
type ToolOutputFileContent struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputFileContent creates a new ToolOutputFileContent instance.
func (m *Module) NewToolOutputFileContent() (*ToolOutputFileContent, error) {
	cls := m.mod.Attr("ToolOutputFileContent")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputFileContent not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputFileContent{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputFileContent) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputFileContent) Object() *pyffi.Object {
	return w.obj
}

// CheckAtLeastOneRequiredField wraps ToolOutputFileContent.check_at_least_one_required_field.
// Validate that at least one of file_data, file_url, or file_id is provided.
func (w *ToolOutputFileContent) CheckAtLeastOneRequiredField() (*ToolOutputFileContent, error) {
	fn := w.obj.Attr("check_at_least_one_required_field")
	if fn == nil {
		return nil, fmt.Errorf("check_at_least_one_required_field not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputFileContent{obj: result, rt: w.rt}, nil
}

// Copy wraps ToolOutputFileContent.copy.
// Returns a copy of the model.
func (w *ToolOutputFileContent) Copy(include any, exclude any, update any, deep bool) (any, error) {
	fn := w.obj.Attr("copy")
	if fn == nil {
		return nil, fmt.Errorf("copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, update, deep)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Dict wraps ToolOutputFileContent.dict.
func (w *ToolOutputFileContent) Dict(include any, exclude any, byAlias bool, excludeUnset bool, excludeDefaults bool, excludeNone bool) (any, error) {
	fn := w.obj.Attr("dict")
	if fn == nil {
		return nil, fmt.Errorf("dict not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, byAlias, excludeUnset, excludeDefaults, excludeNone)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Json wraps ToolOutputFileContent.json.
func (w *ToolOutputFileContent) Json(include any, exclude any, byAlias bool, excludeUnset bool, excludeDefaults bool, excludeNone bool, encoder any, modelsAsDict bool) (string, error) {
	fn := w.obj.Attr("json")
	if fn == nil {
		return "", fmt.Errorf("json not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, byAlias, excludeUnset, excludeDefaults, excludeNone, encoder, modelsAsDict)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ModelCopy wraps ToolOutputFileContent.model_copy.
// !!! abstract "Usage Documentation"
func (w *ToolOutputFileContent) ModelCopy(update any, deep bool) (any, error) {
	fn := w.obj.Attr("model_copy")
	if fn == nil {
		return nil, fmt.Errorf("model_copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(update, deep)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelDump wraps ToolOutputFileContent.model_dump.
// !!! abstract "Usage Documentation"
func (w *ToolOutputFileContent) ModelDump(mode any, include any, exclude any, context any, byAlias any, excludeUnset bool, excludeDefaults bool, excludeNone bool, excludeComputedFields bool, roundTrip bool, warnings any, fallback any, serializeAsAny bool) (any, error) {
	fn := w.obj.Attr("model_dump")
	if fn == nil {
		return nil, fmt.Errorf("model_dump not found")
	}
	defer fn.Close()
	result, err := fn.Call(mode, include, exclude, context, byAlias, excludeUnset, excludeDefaults, excludeNone, excludeComputedFields, roundTrip, warnings, fallback, serializeAsAny)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelDumpJson wraps ToolOutputFileContent.model_dump_json.
// !!! abstract "Usage Documentation"
func (w *ToolOutputFileContent) ModelDumpJson(indent any, ensureAscii bool, include any, exclude any, context any, byAlias any, excludeUnset bool, excludeDefaults bool, excludeNone bool, excludeComputedFields bool, roundTrip bool, warnings any, fallback any, serializeAsAny bool) (string, error) {
	fn := w.obj.Attr("model_dump_json")
	if fn == nil {
		return "", fmt.Errorf("model_dump_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(indent, ensureAscii, include, exclude, context, byAlias, excludeUnset, excludeDefaults, excludeNone, excludeComputedFields, roundTrip, warnings, fallback, serializeAsAny)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ModelPostInit wraps ToolOutputFileContent.model_post_init.
// Override this method to perform additional initialization after `__init__` and `model_construct`.
func (w *ToolOutputFileContent) ModelPostInit(context any) error {
	fn := w.obj.Attr("model_post_init")
	if fn == nil {
		return fmt.Errorf("model_post_init not found")
	}
	defer fn.Close()
	result, err := fn.Call(context)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToolOutputFileContentDict wraps a Python ToolOutputFileContentDict instance.
// TypedDict variant for file content tool outputs.
type ToolOutputFileContentDict struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputFileContentDict creates a new ToolOutputFileContentDict instance.
func (m *Module) NewToolOutputFileContentDict() (*ToolOutputFileContentDict, error) {
	cls := m.mod.Attr("ToolOutputFileContentDict")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputFileContentDict not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputFileContentDict{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputFileContentDict) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputFileContentDict) Object() *pyffi.Object {
	return w.obj
}

// ToolOutputGuardrail wraps a Python ToolOutputGuardrail instance.
// A guardrail that runs after a function tool is invoked.
type ToolOutputGuardrail struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputGuardrail creates a new ToolOutputGuardrail instance.
func (m *Module) NewToolOutputGuardrail(guardrailFunction any, name any) (*ToolOutputGuardrail, error) {
	cls := m.mod.Attr("ToolOutputGuardrail")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputGuardrail not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrailFunction, name)
	if err != nil {
		return nil, err
	}
	return &ToolOutputGuardrail{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputGuardrail) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputGuardrail) Object() *pyffi.Object {
	return w.obj
}

// GetName wraps ToolOutputGuardrail.get_name.
func (w *ToolOutputGuardrail) GetName() (string, error) {
	fn := w.obj.Attr("get_name")
	if fn == nil {
		return "", fmt.Errorf("get_name not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// Run wraps ToolOutputGuardrail.run.
func (w *ToolOutputGuardrail) Run(data *ToolOutputGuardrailData) (*ToolGuardrailFunctionOutput, error) {
	fn := w.obj.Attr("run")
	if fn == nil {
		return nil, fmt.Errorf("run not found")
	}
	defer fn.Close()
	result, err := fn.Call(data)
	if err != nil {
		return nil, err
	}
	return &ToolGuardrailFunctionOutput{obj: result, rt: w.rt}, nil
}

// ToolOutputGuardrailData wraps a Python ToolOutputGuardrailData instance.
// Input data passed to a tool output guardrail function.
type ToolOutputGuardrailData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputGuardrailData creates a new ToolOutputGuardrailData instance.
func (m *Module) NewToolOutputGuardrailData(context any, agent any, output any) (*ToolOutputGuardrailData, error) {
	cls := m.mod.Attr("ToolOutputGuardrailData")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputGuardrailData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(context, agent, output)
	if err != nil {
		return nil, err
	}
	return &ToolOutputGuardrailData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputGuardrailData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputGuardrailData) Object() *pyffi.Object {
	return w.obj
}

// ToolOutputGuardrailResult wraps a Python ToolOutputGuardrailResult instance.
// The result of a tool output guardrail run.
type ToolOutputGuardrailResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputGuardrailResult creates a new ToolOutputGuardrailResult instance.
func (m *Module) NewToolOutputGuardrailResult(guardrail any, output *ToolGuardrailFunctionOutput) (*ToolOutputGuardrailResult, error) {
	cls := m.mod.Attr("ToolOutputGuardrailResult")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputGuardrailResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrail, output)
	if err != nil {
		return nil, err
	}
	return &ToolOutputGuardrailResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputGuardrailResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputGuardrailResult) Object() *pyffi.Object {
	return w.obj
}

// ToolOutputGuardrailTripwireTriggered wraps a Python ToolOutputGuardrailTripwireTriggered instance.
// Exception raised when a tool output guardrail tripwire is triggered.
type ToolOutputGuardrailTripwireTriggered struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputGuardrailTripwireTriggered creates a new ToolOutputGuardrailTripwireTriggered instance.
func (m *Module) NewToolOutputGuardrailTripwireTriggered(guardrail any, output *ToolGuardrailFunctionOutput) (*ToolOutputGuardrailTripwireTriggered, error) {
	cls := m.mod.Attr("ToolOutputGuardrailTripwireTriggered")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputGuardrailTripwireTriggered not found")
	}
	defer cls.Close()
	obj, err := cls.Call(guardrail, output)
	if err != nil {
		return nil, err
	}
	return &ToolOutputGuardrailTripwireTriggered{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputGuardrailTripwireTriggered) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputGuardrailTripwireTriggered) Object() *pyffi.Object {
	return w.obj
}

// ToolOutputImage wraps a Python ToolOutputImage instance.
// Represents a tool output that should be sent to the model as an image.
type ToolOutputImage struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputImage creates a new ToolOutputImage instance.
func (m *Module) NewToolOutputImage() (*ToolOutputImage, error) {
	cls := m.mod.Attr("ToolOutputImage")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputImage not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputImage{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputImage) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputImage) Object() *pyffi.Object {
	return w.obj
}

// CheckAtLeastOneRequiredField wraps ToolOutputImage.check_at_least_one_required_field.
// Validate that at least one of image_url or file_id is provided.
func (w *ToolOutputImage) CheckAtLeastOneRequiredField() (*ToolOutputImage, error) {
	fn := w.obj.Attr("check_at_least_one_required_field")
	if fn == nil {
		return nil, fmt.Errorf("check_at_least_one_required_field not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputImage{obj: result, rt: w.rt}, nil
}

// Copy wraps ToolOutputImage.copy.
// Returns a copy of the model.
func (w *ToolOutputImage) Copy(include any, exclude any, update any, deep bool) (any, error) {
	fn := w.obj.Attr("copy")
	if fn == nil {
		return nil, fmt.Errorf("copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, update, deep)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Dict wraps ToolOutputImage.dict.
func (w *ToolOutputImage) Dict(include any, exclude any, byAlias bool, excludeUnset bool, excludeDefaults bool, excludeNone bool) (any, error) {
	fn := w.obj.Attr("dict")
	if fn == nil {
		return nil, fmt.Errorf("dict not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, byAlias, excludeUnset, excludeDefaults, excludeNone)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Json wraps ToolOutputImage.json.
func (w *ToolOutputImage) Json(include any, exclude any, byAlias bool, excludeUnset bool, excludeDefaults bool, excludeNone bool, encoder any, modelsAsDict bool) (string, error) {
	fn := w.obj.Attr("json")
	if fn == nil {
		return "", fmt.Errorf("json not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, byAlias, excludeUnset, excludeDefaults, excludeNone, encoder, modelsAsDict)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ModelCopy wraps ToolOutputImage.model_copy.
// !!! abstract "Usage Documentation"
func (w *ToolOutputImage) ModelCopy(update any, deep bool) (any, error) {
	fn := w.obj.Attr("model_copy")
	if fn == nil {
		return nil, fmt.Errorf("model_copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(update, deep)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelDump wraps ToolOutputImage.model_dump.
// !!! abstract "Usage Documentation"
func (w *ToolOutputImage) ModelDump(mode any, include any, exclude any, context any, byAlias any, excludeUnset bool, excludeDefaults bool, excludeNone bool, excludeComputedFields bool, roundTrip bool, warnings any, fallback any, serializeAsAny bool) (any, error) {
	fn := w.obj.Attr("model_dump")
	if fn == nil {
		return nil, fmt.Errorf("model_dump not found")
	}
	defer fn.Close()
	result, err := fn.Call(mode, include, exclude, context, byAlias, excludeUnset, excludeDefaults, excludeNone, excludeComputedFields, roundTrip, warnings, fallback, serializeAsAny)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelDumpJson wraps ToolOutputImage.model_dump_json.
// !!! abstract "Usage Documentation"
func (w *ToolOutputImage) ModelDumpJson(indent any, ensureAscii bool, include any, exclude any, context any, byAlias any, excludeUnset bool, excludeDefaults bool, excludeNone bool, excludeComputedFields bool, roundTrip bool, warnings any, fallback any, serializeAsAny bool) (string, error) {
	fn := w.obj.Attr("model_dump_json")
	if fn == nil {
		return "", fmt.Errorf("model_dump_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(indent, ensureAscii, include, exclude, context, byAlias, excludeUnset, excludeDefaults, excludeNone, excludeComputedFields, roundTrip, warnings, fallback, serializeAsAny)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ModelPostInit wraps ToolOutputImage.model_post_init.
// Override this method to perform additional initialization after `__init__` and `model_construct`.
func (w *ToolOutputImage) ModelPostInit(context any) error {
	fn := w.obj.Attr("model_post_init")
	if fn == nil {
		return fmt.Errorf("model_post_init not found")
	}
	defer fn.Close()
	result, err := fn.Call(context)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToolOutputImageDict wraps a Python ToolOutputImageDict instance.
// TypedDict variant for image tool outputs.
type ToolOutputImageDict struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputImageDict creates a new ToolOutputImageDict instance.
func (m *Module) NewToolOutputImageDict() (*ToolOutputImageDict, error) {
	cls := m.mod.Attr("ToolOutputImageDict")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputImageDict not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputImageDict{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputImageDict) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputImageDict) Object() *pyffi.Object {
	return w.obj
}

// ToolOutputText wraps a Python ToolOutputText instance.
// Represents a tool output that should be sent to the model as text.
type ToolOutputText struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputText creates a new ToolOutputText instance.
func (m *Module) NewToolOutputText() (*ToolOutputText, error) {
	cls := m.mod.Attr("ToolOutputText")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputText not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputText{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputText) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputText) Object() *pyffi.Object {
	return w.obj
}

// Copy wraps ToolOutputText.copy.
// Returns a copy of the model.
func (w *ToolOutputText) Copy(include any, exclude any, update any, deep bool) (any, error) {
	fn := w.obj.Attr("copy")
	if fn == nil {
		return nil, fmt.Errorf("copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, update, deep)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Dict wraps ToolOutputText.dict.
func (w *ToolOutputText) Dict(include any, exclude any, byAlias bool, excludeUnset bool, excludeDefaults bool, excludeNone bool) (any, error) {
	fn := w.obj.Attr("dict")
	if fn == nil {
		return nil, fmt.Errorf("dict not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, byAlias, excludeUnset, excludeDefaults, excludeNone)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Json wraps ToolOutputText.json.
func (w *ToolOutputText) Json(include any, exclude any, byAlias bool, excludeUnset bool, excludeDefaults bool, excludeNone bool, encoder any, modelsAsDict bool) (string, error) {
	fn := w.obj.Attr("json")
	if fn == nil {
		return "", fmt.Errorf("json not found")
	}
	defer fn.Close()
	result, err := fn.Call(include, exclude, byAlias, excludeUnset, excludeDefaults, excludeNone, encoder, modelsAsDict)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ModelCopy wraps ToolOutputText.model_copy.
// !!! abstract "Usage Documentation"
func (w *ToolOutputText) ModelCopy(update any, deep bool) (any, error) {
	fn := w.obj.Attr("model_copy")
	if fn == nil {
		return nil, fmt.Errorf("model_copy not found")
	}
	defer fn.Close()
	result, err := fn.Call(update, deep)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelDump wraps ToolOutputText.model_dump.
// !!! abstract "Usage Documentation"
func (w *ToolOutputText) ModelDump(mode any, include any, exclude any, context any, byAlias any, excludeUnset bool, excludeDefaults bool, excludeNone bool, excludeComputedFields bool, roundTrip bool, warnings any, fallback any, serializeAsAny bool) (any, error) {
	fn := w.obj.Attr("model_dump")
	if fn == nil {
		return nil, fmt.Errorf("model_dump not found")
	}
	defer fn.Close()
	result, err := fn.Call(mode, include, exclude, context, byAlias, excludeUnset, excludeDefaults, excludeNone, excludeComputedFields, roundTrip, warnings, fallback, serializeAsAny)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ModelDumpJson wraps ToolOutputText.model_dump_json.
// !!! abstract "Usage Documentation"
func (w *ToolOutputText) ModelDumpJson(indent any, ensureAscii bool, include any, exclude any, context any, byAlias any, excludeUnset bool, excludeDefaults bool, excludeNone bool, excludeComputedFields bool, roundTrip bool, warnings any, fallback any, serializeAsAny bool) (string, error) {
	fn := w.obj.Attr("model_dump_json")
	if fn == nil {
		return "", fmt.Errorf("model_dump_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(indent, ensureAscii, include, exclude, context, byAlias, excludeUnset, excludeDefaults, excludeNone, excludeComputedFields, roundTrip, warnings, fallback, serializeAsAny)
	if err != nil {
		return "", err
	}
	defer result.Close()
	return result.GoString()
}

// ModelPostInit wraps ToolOutputText.model_post_init.
// Override this method to perform additional initialization after `__init__` and `model_construct`.
func (w *ToolOutputText) ModelPostInit(context any) error {
	fn := w.obj.Attr("model_post_init")
	if fn == nil {
		return fmt.Errorf("model_post_init not found")
	}
	defer fn.Close()
	result, err := fn.Call(context)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// ToolOutputTextDict wraps a Python ToolOutputTextDict instance.
// TypedDict variant for text tool outputs.
type ToolOutputTextDict struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolOutputTextDict creates a new ToolOutputTextDict instance.
func (m *Module) NewToolOutputTextDict() (*ToolOutputTextDict, error) {
	cls := m.mod.Attr("ToolOutputTextDict")
	if cls == nil {
		return nil, fmt.Errorf("class ToolOutputTextDict not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &ToolOutputTextDict{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolOutputTextDict) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolOutputTextDict) Object() *pyffi.Object {
	return w.obj
}

// ToolSearchTool wraps a Python ToolSearchTool instance.
// A hosted Responses API tool that lets the model search deferred tools by namespace.
type ToolSearchTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolSearchTool creates a new ToolSearchTool instance.
func (m *Module) NewToolSearchTool(description any, execution any, parameters any) (*ToolSearchTool, error) {
	cls := m.mod.Attr("ToolSearchTool")
	if cls == nil {
		return nil, fmt.Errorf("class ToolSearchTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(description, execution, parameters)
	if err != nil {
		return nil, err
	}
	return &ToolSearchTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolSearchTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolSearchTool) Object() *pyffi.Object {
	return w.obj
}

// ToolTimeoutError wraps a Python ToolTimeoutError instance.
// Exception raised when a function tool invocation exceeds its timeout.
type ToolTimeoutError struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolTimeoutError creates a new ToolTimeoutError instance.
func (m *Module) NewToolTimeoutError(toolName string, timeoutSeconds float64) (*ToolTimeoutError, error) {
	cls := m.mod.Attr("ToolTimeoutError")
	if cls == nil {
		return nil, fmt.Errorf("class ToolTimeoutError not found")
	}
	defer cls.Close()
	obj, err := cls.Call(toolName, timeoutSeconds)
	if err != nil {
		return nil, err
	}
	return &ToolTimeoutError{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolTimeoutError) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolTimeoutError) Object() *pyffi.Object {
	return w.obj
}

// ToolsToFinalOutputResult wraps a Python ToolsToFinalOutputResult instance.
// ToolsToFinalOutputResult(is_final_output: 'bool', final_output: 'Any | None' = None)
type ToolsToFinalOutputResult struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewToolsToFinalOutputResult creates a new ToolsToFinalOutputResult instance.
func (m *Module) NewToolsToFinalOutputResult(isFinalOutput bool, finalOutput any) (*ToolsToFinalOutputResult, error) {
	cls := m.mod.Attr("ToolsToFinalOutputResult")
	if cls == nil {
		return nil, fmt.Errorf("class ToolsToFinalOutputResult not found")
	}
	defer cls.Close()
	obj, err := cls.Call(isFinalOutput, finalOutput)
	if err != nil {
		return nil, err
	}
	return &ToolsToFinalOutputResult{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *ToolsToFinalOutputResult) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *ToolsToFinalOutputResult) Object() *pyffi.Object {
	return w.obj
}

// Trace wraps a Python Trace instance.
// A complete end-to-end workflow containing related spans and metadata.
type Trace struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewTrace creates a new Trace instance.
func (m *Module) NewTrace() (*Trace, error) {
	cls := m.mod.Attr("Trace")
	if cls == nil {
		return nil, fmt.Errorf("class Trace not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &Trace{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Trace) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Trace) Object() *pyffi.Object {
	return w.obj
}

// Export wraps Trace.export.
// Export the trace data as a serializable dictionary.
func (w *Trace) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Finish wraps Trace.finish.
// Finish the trace and optionally reset the current trace.
func (w *Trace) Finish(resetCurrent bool) (any, error) {
	fn := w.obj.Attr("finish")
	if fn == nil {
		return nil, fmt.Errorf("finish not found")
	}
	defer fn.Close()
	result, err := fn.Call(resetCurrent)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Start wraps Trace.start.
// Start the trace and optionally mark it as the current trace.
func (w *Trace) Start(markAsCurrent bool) (any, error) {
	fn := w.obj.Attr("start")
	if fn == nil {
		return nil, fmt.Errorf("start not found")
	}
	defer fn.Close()
	result, err := fn.Call(markAsCurrent)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// ToJson wraps Trace.to_json.
// Serialize trace metadata for persistence or transport.
func (w *Trace) ToJson(includeTracingApiKey bool) (any, error) {
	fn := w.obj.Attr("to_json")
	if fn == nil {
		return nil, fmt.Errorf("to_json not found")
	}
	defer fn.Close()
	result, err := fn.Call(includeTracingApiKey)
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// TracingProcessor wraps a Python TracingProcessor instance.
// Interface for processing and monitoring traces and spans in the OpenAI Agents system.
type TracingProcessor struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewTracingProcessor creates a new TracingProcessor instance.
func (m *Module) NewTracingProcessor() (*TracingProcessor, error) {
	cls := m.mod.Attr("TracingProcessor")
	if cls == nil {
		return nil, fmt.Errorf("class TracingProcessor not found")
	}
	defer cls.Close()
	obj, err := cls.Call()
	if err != nil {
		return nil, err
	}
	return &TracingProcessor{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *TracingProcessor) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *TracingProcessor) Object() *pyffi.Object {
	return w.obj
}

// ForceFlush wraps TracingProcessor.force_flush.
// Forces immediate processing of any queued traces/spans.
func (w *TracingProcessor) ForceFlush() error {
	fn := w.obj.Attr("force_flush")
	if fn == nil {
		return fmt.Errorf("force_flush not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// OnSpanEnd wraps TracingProcessor.on_span_end.
// Called when a span completes execution.
func (w *TracingProcessor) OnSpanEnd(span any) error {
	fn := w.obj.Attr("on_span_end")
	if fn == nil {
		return fmt.Errorf("on_span_end not found")
	}
	defer fn.Close()
	result, err := fn.Call(span)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// OnSpanStart wraps TracingProcessor.on_span_start.
// Called when a new span begins execution.
func (w *TracingProcessor) OnSpanStart(span any) error {
	fn := w.obj.Attr("on_span_start")
	if fn == nil {
		return fmt.Errorf("on_span_start not found")
	}
	defer fn.Close()
	result, err := fn.Call(span)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// OnTraceEnd wraps TracingProcessor.on_trace_end.
// Called when a trace completes execution.
func (w *TracingProcessor) OnTraceEnd(trace *Trace) error {
	fn := w.obj.Attr("on_trace_end")
	if fn == nil {
		return fmt.Errorf("on_trace_end not found")
	}
	defer fn.Close()
	result, err := fn.Call(trace)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// OnTraceStart wraps TracingProcessor.on_trace_start.
// Called when a new trace begins execution.
func (w *TracingProcessor) OnTraceStart(trace *Trace) error {
	fn := w.obj.Attr("on_trace_start")
	if fn == nil {
		return fmt.Errorf("on_trace_start not found")
	}
	defer fn.Close()
	result, err := fn.Call(trace)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// Shutdown wraps TracingProcessor.shutdown.
// Called when the application stops to clean up resources.
func (w *TracingProcessor) Shutdown() error {
	fn := w.obj.Attr("shutdown")
	if fn == nil {
		return fmt.Errorf("shutdown not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// TranscriptionSpanData wraps a Python TranscriptionSpanData instance.
// Represents a Transcription Span in the trace.
type TranscriptionSpanData struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewTranscriptionSpanData creates a new TranscriptionSpanData instance.
func (m *Module) NewTranscriptionSpanData(input any, inputFormat any, output any, model any, modelConfig any) (*TranscriptionSpanData, error) {
	cls := m.mod.Attr("TranscriptionSpanData")
	if cls == nil {
		return nil, fmt.Errorf("class TranscriptionSpanData not found")
	}
	defer cls.Close()
	obj, err := cls.Call(input, inputFormat, output, model, modelConfig)
	if err != nil {
		return nil, err
	}
	return &TranscriptionSpanData{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *TranscriptionSpanData) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *TranscriptionSpanData) Object() *pyffi.Object {
	return w.obj
}

// Export wraps TranscriptionSpanData.export.
// Export the span data as a dictionary.
func (w *TranscriptionSpanData) Export() (any, error) {
	fn := w.obj.Attr("export")
	if fn == nil {
		return nil, fmt.Errorf("export not found")
	}
	defer fn.Close()
	result, err := fn.Call()
	if err != nil {
		return nil, err
	}
	defer result.Close()
	return result.GoValue()
}

// Usage wraps a Python Usage instance.
type Usage struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewUsage creates a new Usage instance.
func (m *Module) NewUsage(DataclassSelf any) (*Usage, error) {
	cls := m.mod.Attr("Usage")
	if cls == nil {
		return nil, fmt.Errorf("class Usage not found")
	}
	defer cls.Close()
	obj, err := cls.Call(DataclassSelf)
	if err != nil {
		return nil, err
	}
	return &Usage{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *Usage) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *Usage) Object() *pyffi.Object {
	return w.obj
}

// Add wraps Usage.add.
// Add another Usage object to this one, aggregating all fields.
func (w *Usage) Add(other *Usage) error {
	fn := w.obj.Attr("add")
	if fn == nil {
		return fmt.Errorf("add not found")
	}
	defer fn.Close()
	result, err := fn.Call(other)
	if err != nil {
		return err
	}
	result.Close()
	return nil
}

// UserError wraps a Python UserError instance.
// Exception raised when the user makes an error using the SDK.
type UserError struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewUserError creates a new UserError instance.
func (m *Module) NewUserError(message string) (*UserError, error) {
	cls := m.mod.Attr("UserError")
	if cls == nil {
		return nil, fmt.Errorf("class UserError not found")
	}
	defer cls.Close()
	obj, err := cls.Call(message)
	if err != nil {
		return nil, err
	}
	return &UserError{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *UserError) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *UserError) Object() *pyffi.Object {
	return w.obj
}

// WebSearchTool wraps a Python WebSearchTool instance.
// A hosted tool that lets the LLM search the web. Currently only supported with OpenAI models,
type WebSearchTool struct {
	obj *pyffi.Object
	rt  *pyffi.Runtime
}

// NewWebSearchTool creates a new WebSearchTool instance.
func (m *Module) NewWebSearchTool(userLocation any, filters any, searchContextSize any) (*WebSearchTool, error) {
	cls := m.mod.Attr("WebSearchTool")
	if cls == nil {
		return nil, fmt.Errorf("class WebSearchTool not found")
	}
	defer cls.Close()
	obj, err := cls.Call(userLocation, filters, searchContextSize)
	if err != nil {
		return nil, err
	}
	return &WebSearchTool{obj: obj, rt: m.rt}, nil
}

// Close releases the underlying Python object.
func (w *WebSearchTool) Close() error {
	return w.obj.Close()
}

// Object returns the underlying Python object.
func (w *WebSearchTool) Object() *pyffi.Object {
	return w.obj
}

// ModelTracing represents the Python ModelTracing enum.
type ModelTracing int

const (
	ModelTracingDisabled ModelTracing = iota
	ModelTracingEnabled
	ModelTracingEnabledWithoutData
)
